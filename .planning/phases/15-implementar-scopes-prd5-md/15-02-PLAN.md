---
phase: 15-implementar-scopes-prd5-md
plan: "02"
type: execute
wave: 2
depends_on:
  - "15-01"
files_modified:
  - frontend/src/components/molecules/Pagination.tsx
autonomous: false
gap_closure: true
requirements:
  - PAGE-02
  - PAGE-07
must_haves:
  truths:
    - "Static PDFs PagedTable footer renders `Rows per page` on a single line immediately before the dropdown."
    - "Document Types PagedTable footer renders `Rows per page` on a single line immediately before the dropdown."
    - "The shared Pagination footer keeps existing rows-per-page behavior and navigation behavior unchanged."
    - "The frontend build completes successfully after the cosmetic fix."
  artifacts:
    - path: "frontend/src/components/molecules/Pagination.tsx"
      provides: "Shared Pagination footer label with no-wrap styling for PagedTable consumers"
      contains: "Rows per page"
  key_links:
    - from: "frontend/src/components/molecules/Pagination.tsx"
      to: "frontend/src/components/atoms/Select.tsx"
      via: "Rows per page label immediately preceding Select"
      pattern: "Rows per page.*Select"
---

<objective>
Close the Phase 15 UAT cosmetic gaps in the shared PagedTable pagination footer.

Purpose: UAT found that the shared `Rows per page` label can wrap before the dropdown in some PagedTable consumers. The fix belongs in `Pagination.tsx` so all current and future PagedTable consumers inherit the corrected footer layout.
Output: A one-file UI styling fix, frontend build verification, and a human UAT re-check prompt for Static PDFs and Document Types.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-implementar-scopes-prd5-md/15-01-PLAN.md
@.planning/phases/15-implementar-scopes-prd5-md/15-01-SUMMARY.md
@.planning/phases/15-implementar-scopes-prd5-md/15-UAT.md
@frontend/src/components/molecules/Pagination.tsx

<interfaces>
Existing shared component contract:

From `frontend/src/components/molecules/Pagination.tsx`:
- `PaginationProps` includes `page`, `pageSize`, `total`, `itemName`, `onChangePage`, optional `pageSizeOptions`, and optional `onChangePageSize`.
- `Pagination` renders the page-size selector only when both `pageSizeOptions` and `onChangePageSize` are present.
- The current selector markup is a `label` containing `<span>Rows per page</span>` followed by the shared `Select` atom.

The UAT root cause is local to the label/span styling: the `Rows per page` text lacks a no-wrap constraint inside the flexible footer.
</interfaces>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Close Phase 15 UAT cosmetic failures for the shared PagedTable footer label. | Covered by Task 1 and Task 2. |
| REQ | PAGE-02 Pagination renders reusable Select-based rows-per-page selector. | Covered by Task 1 without changing selector behavior. |
| REQ | PAGE-07 Frontend build passes. | Covered by Task 2. |
| UAT | Static PDFs gap: `Rows per page` must stay on one line before dropdown. | Covered by Task 1 and human re-check in Task 3. |
| UAT | Document Types gap: shared label must be drawn on one line. | Covered by Task 1 and human re-check in Task 3. |
| CONTEXT | Root cause is `frontend/src/components/molecules/Pagination.tsx`; change only this shared component plus summary output. | Covered by file scope and all tasks. |
| EXCLUDED | Backend pagination, page-size persistence, page consumer rewrites, new UI controls. | Excluded from Task 1 action. |
</source_audit>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add no-wrap styling to the shared Rows per page label</name>
  <files>frontend/src/components/molecules/Pagination.tsx</files>
  <action>In `Pagination.tsx`, update only the page-size selector label styling so the `Rows per page` text remains on a single line immediately before the `Select` dropdown across PagedTable consumers. Add the no-wrap constraint at the smallest effective scope, preferably the `Rows per page` span with Tailwind `whitespace-nowrap`, and preserve the existing label, Select, option rendering, controlled `pageSize` value, `onChangePageSize` conversion, range text, page button logic, and responsive footer structure. Do not modify PagedTable, Static PDFs, Templates, Document Types, backend code, page-size options, or persistence behavior.</action>
  <verify>
    <automated>rtk proxy powershell -NoProfile -Command "Set-Location frontend; npm run build"</automated>
  </verify>
  <done>`Rows per page` cannot wrap independently of the dropdown, the Select remains immediately after the label text, and existing pagination behavior is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Verify frontend build and summarize the gap closure</name>
  <files>.planning/phases/15-implementar-scopes-prd5-md/15-02-SUMMARY.md</files>
  <action>Run the frontend build after the styling fix. Create `15-02-SUMMARY.md` recording the one-file change, the two UAT gaps closed, the build command and result, and that no consumer pages or backend code were changed. Include a short manual UAT checklist requesting re-check of Static PDFs and Document Types footer labels.</action>
  <verify>
    <automated>rtk proxy powershell -NoProfile -Command "Set-Location frontend; npm run build"</automated>
  </verify>
  <done>Frontend build passes and `15-02-SUMMARY.md` documents the cosmetic gap closure and re-check checklist.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UAT re-check the affected PagedTable footer labels</name>
  <what-built>The shared Pagination footer label now has a no-wrap constraint so `Rows per page` stays on one line before the dropdown.</what-built>
  <how-to-verify>
    1. Open Static PDFs and confirm the footer shows `Rows per page` on a single line immediately before the dropdown/select.
    2. Open Document Types and confirm the same single-line label behavior.
    3. Optionally resize the browser to the width where the original cosmetic issue was visible and confirm the label still does not split across lines.
    4. Confirm changing the dropdown value still updates visible rows and existing previous/next navigation still works.
  </how-to-verify>
  <resume-signal>Type "approved" if the labels render correctly, or describe the remaining visual issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser layout engine -> user perception | CSS classes determine whether the already-rendered label is legible and associated with the dropdown. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02-I | Information Disclosure | `Pagination.tsx` footer | accept | Cosmetic no-wrap styling does not change data fetched, displayed, or permissioned. |
| T-15-02-D | Denial of Service | `Pagination.tsx` footer | accept | The change is a static CSS class and does not alter render volume, event handling, or page-size values. |
| T-15-02-E | Elevation of Privilege | PagedTable pagination controls | accept | No auth, route, API, BFF, or backend authorization behavior changes. |
| T-15-02-SC | Tampering | npm package supply chain | accept | No package-manager installs or dependency changes are required. |
</threat_model>

<verification>
Required automated check:

```powershell
rtk proxy powershell -NoProfile -Command "Set-Location frontend; npm run build"
```

Manual UAT re-check:
- Static PDFs: `Rows per page` renders on one line before the dropdown/select.
- Document Types: `Rows per page` renders on one line before the dropdown/select.
- Existing dropdown value changes and pagination navigation still work.
</verification>

<success_criteria>
Both Phase 15 UAT cosmetic gaps are closed by a shared `Pagination.tsx` no-wrap label fix, the frontend build passes, no unrelated files are changed, and the user re-checks the affected PagedTable footer labels.
</success_criteria>

<output>
Create `.planning/phases/15-implementar-scopes-prd5-md/15-02-SUMMARY.md` when done.
</output>
