---
phase: 05-versioning
task: null
total_tasks: null
status: paused
last_updated: "2026-07-08T04:19:27.601Z"
---

<current_state>
Two threads were in flight and got interrupted by context exhaustion, neither superseding the other:

1. **`/gsd:plan-phase 5`** — discuss-phase and research are both done and committed. `05-CONTEXT.md`, `05-DISCUSSION-LOG.md` (commit `f4bb31e`), and `05-RESEARCH.md` (commit `7c67a7c`) all exist. The workflow had NOT yet reached step 5.5 (Validation Strategy check) or spawned `gsd-planner` — no PLAN.md files exist for Phase 5 yet.

2. **`/simplify`** — an ad-hoc review of the already-committed Phase 4 diff (`document_designs.py`, `design_validation.py`, `documentDesigns.ts`, `AddContentModal.tsx`, etc.). All 4 parallel review agents (reuse, simplification, efficiency, altitude) finished and reported 9 distinct findings after dedup. Only the first fix was started — a `require_document_type()` helper was added to `backend/app/api/document_types.py` — but it is **not yet wired into** the two call sites that should use it, and is **uncommitted**.
</current_state>

<completed_work>

- Phase 5 discuss-phase: `05-CONTEXT.md` + `05-DISCUSSION-LOG.md` created — committed `f4bb31e`
- STATE.md session record updated — committed `699ede6`
- Phase 5 plan-phase research: `05-RESEARCH.md` created by `gsd-phase-researcher` — committed `7c67a7c`
- `/simplify` Phase 0 + Phase 1: diff gathered, 4 review agents launched and all completed with findings (see below)
- `/simplify` Phase 2 (partial): added `require_document_type(db, document_type_id)` helper to `backend/app/api/document_types.py` — **uncommitted, not yet used anywhere**
</completed_work>

<remaining_work>

**`/simplify` — 8 remaining findings to apply (or explicitly skip), in `backend/app/api/document_designs.py` unless noted:**

- (A) Wire the new `require_document_type` helper (already added to `document_types.py`) into `document_designs.py:89-91` and `static_pdfs.py:47-51`, replacing the inline `db.query(DocumentType)...` + 404 pattern. Import: `from app.api.document_types import require_document_type`.
- (B) Hoist `jsonOrError`/`readErrorMessage` (duplicated in `frontend/src/lib/documentDesigns.ts:60-75` and `frontend/src/lib/content.ts:56-72`) into `frontend/src/lib/api.ts`; use content.ts's version (handles array `detail`) as the canonical implementation; update both files to import from `api.ts` and delete their local copies.
- (C) Fold repeated page-lookup-or-404 (`update_design_page` line ~237, `delete_design_page` line ~257) into a local `_require_page(design, page_id)` helper in `document_designs.py`.
- (D) Stop re-querying via `_require_design()` after mutation — use the already-loaded/mutated `design` object directly with `_detail(design)` in `create_document_design` (line 103), `reorder_design_pages` (line 225), `activate_document_design` (line 279). Note: `create_document_design` needs `db.refresh(design)` first since it just committed a new row; the other two already have a live, mutated object in scope.
- (E) Fix N+1 query in `design_validation.py`'s `validate_design_activation` (lines 58-67) — currently does one `db.query(HtmlTemplate).filter(...).first()` per page in a loop; batch into a single `db.query(HtmlTemplate).filter(HtmlTemplate.id.in_(ids)).all()` then look up by id in a dict.
- (F) `list_document_designs` (`document_designs.py` lines ~111-129) eager-loads full `pages` via `selectinload` just to compute `page_count = len(design.pages)` — replace with a SQL `func.count` aggregate (subquery or separate grouped count query) so full page JSON isn't pulled for a list view.
- (G) Memoize `existingPdfIds` in `frontend/src/pages/document-designs/DocumentDesignDetailPage.tsx` (lines ~73-75) with `useMemo` — it's currently a new array every render, which is in `AddContentModal.tsx`'s `useEffect` dependency array and triggers unnecessary refetches.
- (H) Move the inline duplicate-PDF check in `add_static_pdf_page` (`document_designs.py` lines 188-192) into `design_validation.py` as `assert_no_duplicate_static_pdf(design, asset)`, alongside its sibling `assert_static_pdf_compatible`/`assert_template_compatible` checks — keeps validation logic in one place.
- (I) `content_templates.py`'s `list_html_templates` lacks the server-side `document_type_id` filter that `static_pdfs.py`'s `list_static_pdf_assets` already has (see `static_pdfs.py:89-104` for the pattern). Add the same optional query param + filter, then simplify `AddContentModal.tsx` (lines ~2070-2074) to call `listHtmlTemplates(documentTypeId)` instead of client-side `.filter(...)`.

After applying (or explicitly skipping with a one-line reason), finish with a summary to the user of what was fixed vs. skipped, per the `/simplify` skill contract.

**`/gsd:plan-phase 5` — resume from where research left off:**

- Step 5.5: Check `05-RESEARCH.md` for a `## Validation Architecture` section; if present, create `05-VALIDATION.md` from the template and commit if `commit_docs` is on.
- Step 5.6: UI Design Contract Gate — should pass quickly since `05-UI-SPEC.md` already exists at `.planning/phases/05-versioning/05-UI-SPEC.md` (note: its own Checker Sign-Off section shows `status: draft` / `Approval: pending` despite STATE.md previously saying "approved" — this discrepancy was noted but not resolved; worth a quick sanity check before treating it as locked).
- Step 6: Confirm no existing PLAN.md files (there are none yet).
- Step 7/7.5: Assemble context paths from INIT; verify Nyquist artifacts.
- Step 8: Spawn `gsd-planner` with the full `planning_context` (reference `05-CONTEXT.md`, `05-RESEARCH.md`, `05-UI-SPEC.md`, plus `backend/app/models/document_design.py`, `DocumentDesignDetailPage.tsx`, `DocumentDesignListPage.tsx`).
- Step 9-13: Handle planner return, spawn `gsd-plan-checker`, revision loop (max 3 iterations), requirements coverage gate.
- Step 14/15: Present final status; auto-advance is off (`workflow._auto_chain_active` and `workflow.auto_advance` both `false`), so route to the manual `<offer_next>` block.
</remaining_work>

<decisions_made>

- Versioning only begins after a design's first activation (D-01/D-02 in `05-CONTEXT.md`) — pre-activation designs behave exactly like Phase 4 (no forking).
- In-progress drafts appear in the Version History table with a "Draft" badge (D-03).
- Only one in-flight draft per design; re-clicking "Edit Design" resumes the existing draft rather than creating a duplicate (D-04).
- Migration: existing `active` Phase 4 designs become "Version 1/Current"; existing never-activated `draft` designs stay version-less (D-05).
- Research recommendation (not yet a locked decision — planner should confirm): extend the `document_designs` table in place with `version_group_id: UUID | None` and `version_number: int | None` columns rather than a new table; extend `DESIGN_STATUSES` to `("draft", "active", "superseded")`; enforce "one active" and "one draft" per version group with two Postgres partial unique indexes; fork by deep-cloning `DocumentDesignPage` rows; flush the old-current→superseded update before the new-draft→active update on activation to avoid a transient unique-index conflict; migrate via a single non-destructive `UPDATE ... SET version_group_id = id, version_number = 1 WHERE status = 'active'`.
- `/simplify` scope was intentionally limited to code/infra files, excluding `.planning/*.md` docs (not code subject to reuse/efficiency review). Fix (A) above deliberately does NOT touch `content_templates.py`'s or `document_types.py`'s own pre-existing DocumentType lookups (they eager-load extra relationships and weren't part of this diff) — only the two newly-added identical lookups in `document_designs.py` and `static_pdfs.py` get the shared helper, to stay within diff scope.
</decisions_made>

<blockers>
None technical. Session paused solely due to context exhaustion, not a stuck problem.
</blockers>

<context>
`docker-compose.yml`, `frontend/src/pages/content/HtmlTemplateCreatePage.tsx`, and the four new Dockerfile/.dockerignore files are pre-existing uncommitted work from *before* this session started (visible in the original git status) — they were reviewed by all 4 `/simplify` agents and found clean, but they are NOT part of this handoff's remaining work; leave them as they are unless the user asks about them separately.

The `require_document_type` helper edit in `document_types.py` is safe as-is (dead code, unused but harmless) if the next session wants to revert it instead of finishing the wiring — no functional risk either way.
</context>

<next_action>
Start with: ask the user whether to (1) finish applying the 8 remaining `/simplify` fixes first, or (2) resume `/gsd:plan-phase 5` first (research is done, next step is spawning `gsd-planner`) — both are independent and either order works, but do not silently drop one.
</next_action>
