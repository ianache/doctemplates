---
phase: 5
slug: versioning
status: draft
shadcn_initialized: false
preset: none
created: 2026-07-07
---

# Phase 5 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

**Phase scope reminder:** This phase delivers `VERSION-01` and `VERSION-02` only — editing an existing document design creates a new version instead of overwriting the previous one, and users can view version history distinguishing past versions from the current one. Phase 5 reuses the Phase 4 interactive designer (page stack, drag-and-drop, inspector, content modals) unchanged for the actual editing surface — do **not** redesign the designer canvas here. Final PDF generation and preview (Phase 6) remain out of scope.

**No `05-CONTEXT.md` exists yet** (no `/gsd:discuss-phase` run for this phase) and no `05-RESEARCH.md` exists. All decisions below are drawn from: (a) the locked platform-wide "Precision Archival" design system already implemented in Phases 1–4, (b) the actual shipped Phase 4 code (`DocumentDesignDetailPage.tsx`, `DesignPageCard.tsx`, `AddContentModal.tsx`, `documentDesigns.ts`), which Phase 4 built without ever producing its own `04-UI-SPEC.md`, and (c) sensible, explicitly-flagged defaults for the version-specific interactions this phase adds. Nothing here required blocking user questions — every open point had an established platform precedent to extend.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (no shadcn) — carried forward from Phases 1–4, no new decision needed |
| Preset | not applicable |
| Component library | none — hand-built Tailwind components against `.design/DESIGN.md`, extending the existing `document-designs` page/component set |
| Icon library | Material Symbols Outlined (already loaded platform-wide, `FILL 0, wght 400, GRAD 0, opsz 24`) |
| Font | Hanken Grotesk (headings) + Inter (body/UI/labels) + JetBrains Mono (technical identifiers, reserved but likely unused this phase) |

**Source:** Continues the locked, platform-wide **"Precision Archival"** design system. No new design-system decision is needed. Phase 5 is additive UI on top of the exact designer scaffolding Phase 4 shipped — same buttons, same card/table/modal chrome, same tokens.

**Discretion note:** Phase 4's `CONTEXT.md` explicitly deferred "editing existing designs" to Phase 5 "so edits can be handled together with versioning" (D-02). This phase's job is therefore: (1) turn the existing read-only "Activate" detail page into one with an "Edit Design" action that forks a new draft version, and (2) add a version-history view. Both reuse existing visual primitives verbatim — no new component category is introduced.

---

## Spacing Scale

Reuses the platform's existing scale (`frontend/tailwind.config.ts` — no new tokens needed):

| Token | Value | Usage |
|-------|-------|-------|
| base | 4px | Icon gaps, hairline offsets |
| xs | 8px | Compact spacing (badge padding, icon-to-label gaps) |
| sm | 12px | Input padding, table/card row vertical padding |
| md | 16px | Default element spacing, card/panel/table-cell padding |
| lg | 24px | Section padding, page content padding, modal padding |
| xl | 32px | Layout gaps, gap between page header and content |
| 2xl | 48px | Major section breaks (empty-state vertical centering) |
| 3xl | 64px | Page-level spacing |

Exceptions: none. (`sm: 12px` is the one platform-wide non-4/8/16 exception, already locked in Phase 1 — not a new exception here.)

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px (`body-md`, Inter) | 400 | 20px (1.43) |
| Label | 11px (`label-caps`, Inter, uppercase, +0.05em tracking) | 700 | 16px (1.45) |
| Heading | 24px (page titles) / 18px (card/modal sub-headings — matches `DesignPageCard`/`AddContentModal` precedent), Hanken Grotesk | 700 | 32px / 28px |
| Code | 12px (`code-sm`, JetBrains Mono) | 400 | 18px (1.5) |

**Weight set: exactly {400, 700}** — same platform-local convention as Phases 1–3 (Inter 400 for Body/Code, 700 for Label and Hanken Grotesk headings).

**Usage mapping for this phase's screens:**
- Page titles ("Version History", `{design name}`) → Heading (24px/700)
- Card-level sub-headings (e.g. read-only banner heading, discard-draft modal title) → Heading (18px/700), matching the existing `DesignPageCard`/`AddContentModal` precedent
- Table column headers, version badges ("Current" / "Superseded" / "Draft"), status pills → Label (11px/700 uppercase)
- Body copy, notice/error banners, metadata values, empty-state copy → Body (14px/400)
- Code role is reserved but not required this phase — only use it if the executor surfaces a raw version/design id for debugging; do not invent a new use for it.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#fff8f6` (`surface` / `background`) | Page background, app shell background |
| Secondary (30%) | `#ffffff` (`surface-container-lowest`) for cards/tables/modals; `#ffe9e6` (`surface-container`) for table header rows and badge fills | Version history table, detail-page cards, modal surface |
| Accent (10%) | `#610000` (`primary`) | Reserved for: "Edit Design" primary button, "Current" version badge text, "Version History" link/button, active designer's "Activate" button (unchanged from Phase 4), input focus border |
| Destructive | `#ba1a1a` (`error`) | Reserved for: "Discard Draft Version" button and its confirmation modal, discard error banners. No other destructive actions exist in this phase — editing itself is non-destructive by design (it forks a version rather than overwriting). |

Accent reserved for: "Edit Design" button, "Current" badge, "Version History" navigation control, input focus border — never for "Superseded" badges, table row backgrounds, or the read-only-viewing banner (those stay neutral, per below).

**Version badges (new this phase):** Both badges reuse the exact pill shape already established for design status (`rounded bg-surface-container px-sm py-xs text-sm font-bold ...`) so the platform doesn't introduce a new badge shape — only the text color changes:
- **Current** → `text-primary` (accent) — the only version-state badge allowed to use accent color.
- **Superseded** → `text-on-surface-variant` (neutral) — every past version, regardless of how far back.
- **Draft** (an in-progress edit that hasn't been activated yet) → reuse the existing `design.status` pill verbatim (already implemented as `text-primary` for the generic status pill in `DocumentDesignDetailPage.tsx`); do not add a second accent-colored badge next to "Current" for the same version — a version being edited is never simultaneously "Current," so there is no visual collision.

**Text-on-surface:** `#261816` (`on-surface`) primary text; `#5a403c` (`on-surface-variant`) secondary/muted text; `#5e5e5e` (`secondary`) tertiary text (table column headers, empty-state icon) — all unchanged from prior phases.

**Borders:** `#e3beb8` (`outline-variant`) default hairlines; `#8e706b` (`outline`) stronger borders — unchanged.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA — detail page, viewing current version | **"Edit Design"** — forks a new draft version cloned from the current one and opens it in the existing Phase 4 designer |
| Secondary action — detail page | **"Version History"** — navigates to this design's version list |
| Notice — after clicking Edit Design | "New draft created from version {n}. Changes here won't affect the current version until you activate this one." |
| Notice — after activating a new version | "Version {n} is now current. Version {n-1} has been preserved in version history." |
| Version history page title | "Version History" |
| Version history page helper text | "Every saved edit to this design is preserved here. The current version is what generation and preview use." |
| Version badge — current | "Current" |
| Version badge — past | "Superseded" |
| Version history table columns | Version, State, Status, Created By, Created At |
| Version history row action | "View" (opens that version read-only, or the live designer if it's the current version) |
| Read-only past-version banner | "You're viewing version {n} — a past version and read-only. [View current version]" |
| Empty state — version history (defensive; should not normally occur, a design always has ≥1 version) | Heading: "No versions found." Body: "This design has no recorded versions yet." |
| Error state — version history load failure | "We couldn't load version history. Try again." |
| Error state — edit/fork failure | "We couldn't create a new version. Try again." |
| Destructive action — discard an in-progress draft version | Button: **"Discard Draft Version"**. Confirmation modal title: "Discard this draft?" Body: "Changes since version {n} will be lost. Version {n} itself stays intact and remains the current version." Buttons: "Cancel" / "Discard Draft" (destructive, error-red) |

**Language:** English for all UI chrome/copy, matching every prior phase's precedent.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none — shadcn not initialized this phase | not applicable |
| third-party | none | not applicable |

---

## Layout Notes (Phase 5 specific)

- **Detail page (current version) changes:** Extend the existing `DocumentDesignDetailPage.tsx` action row (currently just "Activate") with two additions, in this order left-to-right: "Edit Design" (primary, accent) and "Version History" (secondary, outline — same visual weight as the existing "Add PDF" button). Only show "Edit Design" when the design being viewed *is* the current version. Do not show it on a read-only past-version view.
- **Editing flow:** Clicking "Edit Design" immediately creates a new draft version (a full clone of the current version's pages) and navigates the user into that draft's detail/designer page — i.e. the exact same `DocumentDesignDetailPage` UI Phase 4 already built (page stack, drag-and-drop, inspector, Add Template/Add PDF, Activate). No new designer chrome is introduced. Show the "New draft created…" notice (see Copywriting Contract) using the existing notice-banner classes (`mt-md flex ... rounded border border-outline-variant bg-surface-container-lowest p-sm text-sm text-on-surface`) once, on arrival.
- **Discard draft:** While editing an un-activated draft version (i.e. a version created via "Edit Design" that hasn't been activated), show a "Discard Draft Version" destructive button near "Activate". Clicking it opens a confirmation modal reusing the exact modal shell already established in `AddContentModal.tsx` (`fixed inset-0 z-50 flex items-center justify-center bg-black/30`, card `w-full max-w-xl rounded border border-outline-variant bg-surface-container-lowest p-lg shadow-xl`), with the copy from the Copywriting Contract above. Confirming returns the user to the version history page (the draft is gone; the current version is untouched).
- **Version History page (new):** A dedicated route (e.g. `/document-designs/{id}/versions`), reached via the "Version History" button on the detail page — not a tab, consistent with the platform's existing list/detail page-per-route pattern (no tabbed interfaces exist anywhere else in the app). Layout: page header (Heading + helper text, matching `DocumentDesignListPage`'s header treatment) with a "Back to Design" link. Below it, a single table reusing the exact table chrome from `DocumentDesignListPage.tsx` (`rounded border border-outline-variant bg-surface-container-lowest`, `bg-surface-container` header row, `text-[11px] font-bold uppercase text-secondary` column labels). Rows sorted newest-first. The current version's row uses the "Current" badge; every other row uses "Superseded."
- **Read-only past-version view:** Reuse `DocumentDesignDetailPage` for viewing a past version, but strip all mutating affordances: no drag handles, no "Remove" buttons on page cards, no Add Template/Add PDF buttons, no Activate/Edit Design/Discard buttons, no inspector edit form (render inspector fields as plain read-only text). Show the read-only banner (see Copywriting Contract) at the top, directly under the page title, using the same notice-banner visual treatment as elsewhere. Provide a "View current version" link inside that banner back to the live detail page.
- **Icons:** Material Symbols Outlined only, consistent sizing with existing usage (`history` for the Version History button/page, `edit` for Edit Design, `delete` for Discard Draft Version — reusing the same `delete` icon already used for page removal in `DesignPageCard.tsx`).

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
