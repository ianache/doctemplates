---
phase: 14-implementar-scopes-prd4-md
plan: "05"
type: execute
wave: 4
depends_on:
  - 14-03
  - 14-04
files_modified:
  - backend/tests/test_async_generation_jobs.py
  - backend/tests/test_generation_preview.py
  - backend/tests/test_issuance_library_api.py
  - backend/tests/test_document_tracelogs.py
  - bff/tests/test_proxy.py
autonomous: true
requirements:
  - ASYNC-01
  - ASYNC-02
  - ASYNC-03
  - ASYNC-04
  - ASYNC-05
  - ASYNC-06
  - JOBUI-01
  - JOBUI-02
  - JOBUI-03
must_haves:
  truths:
    - "Backend tests prove enqueue, worker success, worker failure, idempotency, and download-before-complete conflicts."
    - "BFF tests prove async 202 and 409 responses pass through."
    - "Frontend build proves async status types and polling UI compile."
    - "Docker Compose config includes Redis and worker services with shared local storage."
  artifacts:
    - backend/tests/test_async_generation_jobs.py
    - backend/tests/test_generation_preview.py
    - backend/tests/test_issuance_library_api.py
    - backend/tests/test_document_tracelogs.py
    - bff/tests/test_proxy.py
  key_links:
    - from: "backend/tests/test_async_generation_jobs.py"
      to: "backend/app/workers/document_generation.py"
      via: "direct worker task invocation/eager mode"
      pattern: "generate_document_pdf"
    - from: "backend/tests/test_generation_preview.py"
      to: "backend/app/api/document_designs.py"
      via: "POST generate returns 202"
      pattern: "202"
    - from: "backend/tests/test_issuance_library_api.py"
      to: "backend/app/api/issuances.py"
      via: "409 readiness guard coverage"
      pattern: "409"
---

<objective>
Add cross-layer verification for async document generation jobs.

Purpose: Prove Phase 14 works across backend API, worker lifecycle, BFF proxy behavior, frontend compile-time contracts, and Docker Compose runtime configuration.
Output: Focused tests and verification commands covering all Phase 14 requirements.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-implementar-scopes-prd4-md/14-CONTEXT.md
@.planning/phases/14-implementar-scopes-prd4-md/14-RESEARCH.md
@.scopes/PRD4.md
@.planning/v2.0-v2.0-MILESTONE-AUDIT.md
@backend/tests/conftest.py
@backend/tests/test_generation_preview.py
@backend/tests/test_issuance_library_api.py
@backend/tests/test_document_tracelogs.py
@backend/tests/test_storage_providers.py
@bff/tests/test_proxy.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Add backend async job lifecycle tests</name>
  <files>
    <file>backend/tests/test_async_generation_jobs.py</file>
    <file>backend/tests/test_generation_preview.py</file>
    <file>backend/tests/test_issuance_library_api.py</file>
    <file>backend/tests/test_document_tracelogs.py</file>
  </files>
  <read_first>
    <file>backend/tests/conftest.py</file>
    <file>backend/tests/test_generation_preview.py</file>
    <file>backend/tests/test_issuance_library_api.py</file>
    <file>backend/tests/test_document_tracelogs.py</file>
    <file>backend/app/api/document_designs.py</file>
    <file>backend/app/api/issuances.py</file>
    <file>backend/app/workers/document_generation.py</file>
  </read_first>
  <behavior>
    - Enqueue test: POST generate creates queued issuance, stores task id, returns 202, and does not call `generate_composed_pdf` in the request.
    - Worker success test: queued issuance becomes processing then success with `storage_key`, `completed_at`, and generation tracelog.
    - Worker failure test: generation/storage exception marks failure with bounded `error_message` and `completed_at`.
    - Worker idempotency test: non-queued issuance exits without regenerating or changing terminal state.
    - Endpoint guard tests: download, preview/public download, and share-before-success return 409 for queued, processing, and failure states.
  </behavior>
  <action>
    Create `backend/tests/test_async_generation_jobs.py` for worker-focused tests using the project's existing database/test fixtures. Use Celery eager mode or direct task invocation so tests are deterministic and do not require a live Redis process.
    Extend `test_generation_preview.py` to assert the generate endpoint returns 202 with status queued, nullable storage key, queued timestamp, persisted input/metadata/user/design version, and stored Celery task id. Monkeypatch the enqueue seam rather than running a real worker in API tests.
    Extend `test_issuance_library_api.py` for status filters including queued and processing, detail serialization of job fields, and HTTP 409 readiness guards for download/share/public download where those routes live.
    Extend `test_document_tracelogs.py` only for success tracelog behavior preserved by the worker. Do not add a new failure tracelog event unless implementation changed the trace event constraint and frontend label mapping.
  </action>
  <verify>
    <automated>rtk proxy powershell -NoProfile -Command "Set-Location backend; uv run pytest tests/test_async_generation_jobs.py tests/test_generation_preview.py tests/test_issuance_library_api.py tests/test_document_tracelogs.py -q"</automated>
  </verify>
  <done>
    <criterion>Backend tests cover ASYNC-01 through ASYNC-06 with deterministic worker execution.</criterion>
  </done>
</task>

<task type="auto">
  <name>Run BFF, frontend, backend, and compose verification suite</name>
  <files>
    <file>bff/tests/test_proxy.py</file>
  </files>
  <read_first>
    <file>bff/tests/test_proxy.py</file>
    <file>docker-compose.yml</file>
    <file>frontend/package.json</file>
    <file>backend/pyproject.toml</file>
  </read_first>
  <action>
    Run the BFF proxy tests after plan 14-04 changes.
    Run the frontend build after job UI changes.
    Run the focused backend async tests, then the full backend suite if the focused suite passes.
    Run `docker compose config` to prove Redis and worker compose definitions are valid.
    If Docker is available in the execution environment, run a smoke check with `docker compose up -d postgres redis backend worker bff frontend`, verify `docker compose ps` shows the worker running, and inspect worker logs for successful Celery startup. Stop only services started by this task if the executor started them for the smoke check.
  </action>
  <verify>
    <automated>rtk proxy powershell -NoProfile -Command "Set-Location bff; uv run pytest -q"</automated>
    <automated>rtk proxy powershell -NoProfile -Command "Set-Location frontend; npm run build"</automated>
    <automated>rtk proxy powershell -NoProfile -Command "Set-Location backend; uv run pytest -q"</automated>
    <automated>rtk docker compose config</automated>
  </verify>
  <done>
    <criterion>BFF tests, frontend build, backend tests, and compose config all pass.</criterion>
    <criterion>When Docker runtime is available, Redis and worker services start with the application stack.</criterion>
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test client -> Backend/BFF | Automated tests exercise authenticated and proxied API behavior. |
| Worker tests -> StorageProvider | Tests prove worker writes or rejects storage operations consistently. |
| Compose runtime -> services | Docker starts Redis, backend, worker, BFF, frontend, and shared storage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05-01 | Tampering | async lifecycle regression | mitigate | Tests assert exact lifecycle states, task id persistence, and idempotency. |
| T-14-05-02 | Information Disclosure | unfinished share/download | mitigate | Tests assert 409 guards for non-success states, including public download. |
| T-14-05-03 | Denial of Service | missing Redis/worker runtime | mitigate | Compose config and optional runtime smoke verify Redis and worker are wired. |
| T-14-05-SC | Tampering | package-manager installs | accept | No new package install is planned in this verification plan. |
</threat_model>

<verification>
Required commands:
- `rtk proxy powershell -NoProfile -Command "Set-Location backend; uv run pytest tests/test_async_generation_jobs.py tests/test_generation_preview.py tests/test_issuance_library_api.py tests/test_document_tracelogs.py -q"`
- `rtk proxy powershell -NoProfile -Command "Set-Location bff; uv run pytest -q"`
- `rtk proxy powershell -NoProfile -Command "Set-Location frontend; npm run build"`
- `rtk proxy powershell -NoProfile -Command "Set-Location backend; uv run pytest -q"`
- `rtk docker compose config`
</verification>

<success_criteria>
Phase 14 has automated proof across backend API, worker lifecycle, BFF proxying, frontend job UI contracts, and Docker Compose runtime configuration.
</success_criteria>

<output>
Create `.planning/phases/14-implementar-scopes-prd4-md/14-05-SUMMARY.md` when done.
</output>
