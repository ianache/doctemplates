---
phase: 13-implementar-la-propuesta-de-separacion-de-almacenamiento
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/static_pdf_asset.py
  - backend/app/models/document_issuance.py
  - backend/alembic/versions/0012_storage_decoupling.py
autonomous: true
requirements:
  - STORAGE-02
must_haves:
  truths:
    - "Database schema includes storage_key instead of stored_path in static_pdf_assets table."
    - "Database schema includes storage_key instead of file_path in document_issuances table."
    - "Data migration extracts basename from absolute paths for existing records."
  artifacts:
    - backend/app/models/static_pdf_asset.py
    - backend/app/models/document_issuance.py
    - backend/alembic/versions/0012_storage_decoupling.py
  key_links: []
---

<objective>
Rename columns and modify models for storage decoupling:
1. Rename `stored_path` to `storage_key` in `static_pdf_assets` table.
2. Rename `file_path` to `storage_key` in `document_issuances` table.
3. Perform data migration to extract basenames from absolute paths for existing database entries so they function as logical keys.
4. Update `StaticPdfAsset` and `DocumentIssuance` SQLAlchemy models.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.design/storage_decoupling_proposal.md
@.planning/phases/13-implementar-la-propuesta-de-separacion-de-almacenamiento/13-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Generate and implement Alembic migration</name>
  <files>
    <file>backend/alembic/versions/0012_storage_decoupling.py</file>
  </files>
  <read_first>
    <file>backend/alembic/versions/0011_template_css.py</file>
  </read_first>
  <action>
    Create Alembic migration script by running 'uv run alembic revision -m "storage_decoupling"' in backend.
    Rename the generated file to '0012_storage_decoupling.py' to preserve the project's sequence.
    Implement the upgrade and downgrade functions.
    In upgrade:
      - Rename column 'stored_path' to 'storage_key' on table 'static_pdf_assets'.
      - Rename column 'file_path' to 'storage_key' on table 'document_issuances'.
      - Migrate existing records: Extract the base filename (e.g., using SQL 'split_part' or standard replacement/regex) from the path columns (e.g., '/app/data/abc.pdf' becomes 'abc.pdf').
    In downgrade:
      - Rename column 'storage_key' back to 'stored_path' on table 'static_pdf_assets'.
      - Rename column 'storage_key' back to 'file_path' on table 'document_issuances'.
  </action>
  <verify>
    <automated>cd backend && uv run alembic upgrade head && uv run alembic downgrade -1 && uv run alembic upgrade head</automated>
  </verify>
  <done>
    <criterion>The migration script runs successfully for both upgrade and downgrade steps on the local database.</criterion>
  </done>
</task>

<task type="auto">
  <name>Update static asset and issuance SQLAlchemy models</name>
  <files>
    <file>backend/app/models/static_pdf_asset.py</file>
    <file>backend/app/models/document_issuance.py</file>
  </files>
  <read_first>
    <file>backend/app/models/static_pdf_asset.py</file>
    <file>backend/app/models/document_issuance.py</file>
  </read_first>
  <action>
    Update static_pdf_asset.py to rename the model attribute 'stored_path' to 'storage_key'.
    Update document_issuance.py to rename the model attribute 'file_path' to 'storage_key'.
  </action>
  <verify>
    <automated>cd backend && uv run python -c "from app.models.static_pdf_asset import StaticPdfAsset; from app.models.document_issuance import DocumentIssuance; assert hasattr(StaticPdfAsset, 'storage_key') and hasattr(DocumentIssuance, 'storage_key')"</automated>
  </verify>
  <done>
    <criterion>SQLAlchemy model definitions compiled successfully with renamed fields.</criterion>
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Database Engine | Internal storage engine accessing database schema |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01-01 | Tampering | database-migration | medium | mitigate | Run alembic upgrade and downgrade checks to verify integrity |
| T-13-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
Verify database migrations execute correctly forwards and backwards. Verify SQLAlchemy models import and reflect the updated columns.
</verification>

<success_criteria>
The database has storage_key columns in static_pdf_assets and document_issuances tables. Existing records have their paths migrated to base filenames. Models compile and load with renamed columns.
</success_criteria>

<output>
Create `.planning/phases/13-implementar-la-propuesta-de-separacion-de-almacenamiento/13-01-SUMMARY.md` when done
</output>
