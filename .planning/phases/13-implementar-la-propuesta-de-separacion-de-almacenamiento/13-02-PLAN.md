---
phase: 13-implementar-la-propuesta-de-separacion-de-almacenamiento
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/config.py
  - backend/app/services/storage/base.py
  - backend/app/services/storage/local.py
  - backend/app/dependencies.py
autonomous: true
requirements:
  - STORAGE-01
  - STORAGE-03
must_haves:
  truths:
    - "StorageProvider abstract class defines required interface methods."
    - "LocalStorageProvider implements the defined StorageProvider interface for local filesystem."
    - "config.py exposes new configurations with sensible defaults."
    - "dependencies.py exposes get_storage_provider returning a LocalStorageProvider by default."
  artifacts:
    - backend/app/services/storage/base.py
    - backend/app/services/storage/local.py
    - backend/app/dependencies.py
  key_links: []
---

<objective>
Define the core storage provider abstractions, the local filesystem provider implementation, the backend configuration schema, and the dependency injection wiring.
1. Define abstract base class `StorageProvider`.
2. Implement `LocalStorageProvider` for filesystem compatibility.
3. Update `config.py` with variables for configuring providers.
4. Expose `get_storage_provider` dependency injector in `app/dependencies.py`.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.design/storage_decoupling_proposal.md
@.planning/phases/13-implementar-la-propuesta-de-separacion-de-almacenamiento/13-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Create StorageProvider interface and LocalStorageProvider implementation</name>
  <files>
    <file>backend/app/services/storage/base.py</file>
    <file>backend/app/services/storage/local.py</file>
  </files>
  <read_first>
    <file>backend/app/config.py</file>
  </read_first>
  <action>
    Create 'backend/app/services/storage/base.py' and define:
      - 'StorageProvider' ABC class.
      - Abstract methods: 'save', 'get', 'get_stream', 'delete', and 'get_download_response'.
    Create 'backend/app/services/storage/local.py' and implement 'LocalStorageProvider' extending 'StorageProvider':
      - Resolve categories 'static_pdfs' and 'issuances' to local directory paths configured in Settings.
      - Automatically create subdirectories if they do not exist on write.
      - Return 'FileResponse' inside 'get_download_response' method.
  </action>
  <verify>
    <automated>cd backend && uv run python -c "from app.services.storage.local import LocalStorageProvider; print('Storage classes loaded successfully')"</automated>
  </verify>
  <done>
    <criterion>Storage base and local provider source files exist and compile cleanly.</criterion>
  </done>
</task>

<task type="auto">
  <name>Update config.py and implement get_storage_provider dependency</name>
  <files>
    <file>backend/app/config.py</file>
    <file>backend/app/dependencies.py</file>
  </files>
  <read_first>
    <file>backend/app/config.py</file>
  </read_first>
  <action>
    Update 'backend/app/config.py' settings to include:
      - 'storage_provider_type': str = "local"
      - 'storage_s3_endpoint_url': str | None = None
      - 'storage_s3_access_key': str | None = None
      - 'storage_s3_secret_key': str | None = None
      - 'storage_s3_region': str | None = None
      - 'storage_s3_bucket_static_pdfs': str = "docmanagement-static-pdfs"
      - 'storage_s3_bucket_issuances': str = "docmanagement-issuances"
    Create 'backend/app/dependencies.py' and implement 'get_storage_provider' cached function returning the active provider:
      - Instantiates LocalStorageProvider by default with root paths from settings.
      - Stub/raise error if 'storage_provider_type == "s3"' until S3 provider is implemented in Wave 2.
  </action>
  <verify>
    <automated>cd backend && uv run python -c "from app.dependencies import get_storage_provider; provider = get_storage_provider(); print(type(provider))"</automated>
  </verify>
  <done>
    <criterion>App configs are updated and dependencies.py returns LocalStorageProvider instance successfully.</criterion>
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Configuration Loading | System boundaries reading configurations from environment variables |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-02-01 | Information Disclosure | configuration | high | mitigate | Protect secrets by using optional environment values and avoiding hardcoded secrets |
| T-13-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
Verify config variables are accessible. Verify dependency injection yields the LocalStorageProvider when configured to 'local'.
</verification>

<success_criteria>
LocalStorageProvider is defined and handles standard filesystem calls. Config and dependencies files are created/modified and successfully run in the Python backend.
</success_criteria>

<output>
Create `.planning/phases/13-implementar-la-propuesta-de-separacion-de-almacenamiento/13-02-SUMMARY.md` when done
</output>
