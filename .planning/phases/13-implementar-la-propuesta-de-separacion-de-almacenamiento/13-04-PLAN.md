---
phase: 13-implementar-la-propuesta-de-separacion-de-almacenamiento
plan: "04"
type: execute
wave: 2
depends_on:
  - "13-02"
files_modified:
  - backend/app/services/storage/s3.py
  - backend/app/dependencies.py
  - .env.example
  - .env
autonomous: true
requirements:
  - STORAGE-03
must_haves:
  truths:
    - "S3StorageProvider is defined and handles AWS S3-compatible operations via boto3."
    - "S3StorageProvider get_download_response streams bytes directly using fastapi StreamingResponse."
    - ".env and .env.example have STORAGE_PROVIDER_TYPE and STORAGE_S3_* configurations."
  artifacts:
    - backend/app/services/storage/s3.py
    - .env.example
  key_links: []
---

<objective>
Implement the S3StorageProvider concrete implementation for S3-compatible object storage (AWS, MinIO, OCI) and update configuration templates.
1. Add environment variables for S3 credentials and settings to `.env` and `.env.example`.
2. Implement `S3StorageProvider` using `boto3`.
3. Update `dependencies.py` to instantiate and return `S3StorageProvider` when configured.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.design/storage_decoupling_proposal.md
@.planning/phases/13-implementar-la-propuesta-de-separacion-de-almacenamiento/13-CONTEXT.md
@.planning/phases/13-implementar-la-propuesta-de-separacion-de-almacenamiento/13-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Add S3 environment configuration variables</name>
  <files>
    <file>.env.example</file>
    <file>.env</file>
  </files>
  <read_first>
    <file>.env.example</file>
  </read_first>
  <action>
    Add the following configuration lines to '.env.example' and local '.env':
      - STORAGE_PROVIDER_TYPE=local  # 'local' or 's3'
      - STORAGE_S3_ENDPOINT_URL=     # e.g., http://localhost:9000 for MinIO
      - STORAGE_S3_ACCESS_KEY=
      - STORAGE_S3_SECRET_KEY=
      - STORAGE_S3_REGION=us-east-1
      - STORAGE_S3_BUCKET_STATIC_PDFS=docmanagement-static-pdfs
      - STORAGE_S3_BUCKET_ISSUANCES=docmanagement-issuances
  </action>
  <verify>
    <automated>grep -q "STORAGE_PROVIDER_TYPE" .env.example &amp;&amp; grep -q "STORAGE_PROVIDER_TYPE" .env</automated>
  </verify>
  <done>
    <criterion>S3-related environment variables are defined in .env and .env.example.</criterion>
  </done>
</task>

<task type="auto">
  <name>Implement S3StorageProvider using boto3</name>
  <files>
    <file>backend/app/services/storage/s3.py</file>
    <file>backend/app/dependencies.py</file>
  </files>
  <read_first>
    <file>backend/app/services/storage/base.py</file>
    <file>backend/app/dependencies.py</file>
  </read_first>
  <action>
    Install 'boto3' package using 'uv add boto3' in backend directory (if not present).
    Create 'backend/app/services/storage/s3.py' and implement 'S3StorageProvider' inheriting from 'StorageProvider':
      - Initialize S3 client using boto3.client with Config(signature_version="s3v4").
      - Implement 'save', 'get', 'get_stream', 'delete' operations using 'put_object', 'get_object', and 'delete_object'.
      - Implement 'get_download_response' returning a 'StreamingResponse' that streams from the S3 get_object body (setting the appropriate 'Content-Disposition' header).
    Update 'backend/app/dependencies.py' to instantiate S3StorageProvider when 'settings.storage_provider_type == "s3"'.
  </action>
  <verify>
    <automated>cd backend && uv run python -c "from app.services.storage.s3 import S3StorageProvider; print('S3StorageProvider loaded successfully')"</automated>
  </verify>
  <done>
    <criterion>S3StorageProvider is defined and dependencies.py returns it on demand.</criterion>
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend App → AWS S3 / MinIO | Connection to cloud/external object storage provider |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-04-01 | Information Disclosure | s3-connections | high | mitigate | Use v4 signatures and restrict credential exposure |
| T-13-04-02 | Elevation of Privilege | s3-buckets | medium | mitigate | Enforce StreamingResponse instead of public bucket redirects to keep buckets private |
| T-13-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
Verify S3StorageProvider loads correctly. Ensure boto3 is installed as a production dependency in the backend pyproject.toml.
</verification>

<success_criteria>
S3StorageProvider is fully implemented and loads without module errors. Config changes are documented in .env.example.
</success_criteria>

<output>
Create `.planning/phases/13-implementar-la-propuesta-de-separacion-de-almacenamiento/13-04-SUMMARY.md` when done
</output>
