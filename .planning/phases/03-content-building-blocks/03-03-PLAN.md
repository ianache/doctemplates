---
phase: 03-content-building-blocks
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - frontend/src/App.tsx
  - frontend/src/pages/AuthenticatedShell.tsx
  - frontend/src/lib/content.ts
  - frontend/src/pages/content/ContentLibraryPage.tsx
  - frontend/src/pages/content/HtmlTemplateCreatePage.tsx
  - frontend/src/pages/content/HtmlTemplateDetailPage.tsx
  - frontend/src/pages/content/StaticPdfUploadPage.tsx
  - frontend/src/pages/content/StaticPdfDetailPage.tsx
autonomous: false
requirements: [CONTENT-01, CONTENT-02, CONTENT-03, VALID-01]

must_haves:
  truths:
    - "Users can browse content blocks in the authenticated shell"
    - "Users can author an HTML template against a document type from the UI"
    - "Users can upload a static PDF from the UI"
    - "The UI surfaces token-validation failures clearly"
    - "The UI surfaces stored PDF metadata and download access clearly"
  artifacts:
    - path: frontend/src/lib/content.ts
      provides: "Typed API wrappers for templates and PDF assets"
      contains: "listHtmlTemplates"
    - path: frontend/src/pages/content/ContentLibraryPage.tsx
      provides: "Entry page / tabbed library shell for content blocks"
      contains: "Templates"
    - path: frontend/src/pages/content/HtmlTemplateCreatePage.tsx
      provides: "Template authoring form with document-type selection and HTML editor"
      contains: "Create Template"
    - path: frontend/src/pages/content/StaticPdfUploadPage.tsx
      provides: "Static PDF upload form"
      contains: "Upload PDF"
    - path: frontend/src/pages/AuthenticatedShell.tsx
      provides: "Updated nav entries for content blocks"
      contains: "Document Types"
  key_links:
    - from: frontend/src/pages/content/HtmlTemplateCreatePage.tsx
      to: frontend/src/lib/content.ts
      via: "createHtmlTemplate"
      pattern: "createHtmlTemplate\\("
    - from: frontend/src/pages/content/StaticPdfUploadPage.tsx
      to: frontend/src/lib/content.ts
      via: "uploadStaticPdfAsset"
      pattern: "uploadStaticPdfAsset\\("
    - from: frontend/src/App.tsx
      to: frontend/src/pages/AuthenticatedShell.tsx
      via: "nested <Route> + <Outlet/>"
      pattern: "Outlet"
---

<objective>
Build the Phase 3 content-library UI: browsing, template authoring, and static-PDF upload pages wired to the backend APIs from 03-01 and 03-02.

Purpose: Gives operational users the browser surface needed to create and inspect the content blocks that Phase 4 will compose into designs, while keeping the UI intentionally simple and admin-focused.

Output: A working content library section in the authenticated shell, plus a browser-confirmed manual verification of valid template save, invalid token rejection, PDF upload, and PDF browsing.
</objective>

<execution_context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-content-building-blocks/03-CONTEXT.md
@.planning/phases/03-content-building-blocks/03-RESEARCH.md
@.planning/phases/03-content-building-blocks/03-VALIDATION.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Content library routing, shell navigation, and API client</name>
  <files>frontend/src/App.tsx, frontend/src/pages/AuthenticatedShell.tsx, frontend/src/lib/content.ts</files>
  <read_first>frontend/src/App.tsx, frontend/src/pages/AuthenticatedShell.tsx, frontend/src/lib/api.ts, frontend/package.json</read_first>
  <action>
Add nested routing for the content library, extend the authenticated shell nav, and create typed API wrappers for template and PDF endpoints.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Content navigation exists in the authenticated shell
    - The new content lib compiles against the backend response shapes
    - The frontend build passes
  </acceptance_criteria>
  <done>The shell can route to content-library pages and the typed client exists for later forms.</done>
</task>

<task type="auto">
  <name>Task 2: Template authoring and PDF upload pages</name>
  <files>frontend/src/pages/content/ContentLibraryPage.tsx, frontend/src/pages/content/HtmlTemplateCreatePage.tsx, frontend/src/pages/content/HtmlTemplateDetailPage.tsx, frontend/src/pages/content/StaticPdfUploadPage.tsx, frontend/src/pages/content/StaticPdfDetailPage.tsx</files>
  <read_first>.planning/phases/03-content-building-blocks/03-UI-SPEC.md, frontend/src/lib/content.ts, frontend/src/lib/documentTypes.ts</read_first>
  <action>
Create the operational library pages: a template authoring form with document-type selection, a template detail/browse page, a static-PDF upload form, and a static-PDF detail/browse page.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Template authoring UI exists and can submit content
    - Static PDF upload UI exists and can submit content
    - Detail pages render stored metadata and actions
    - The frontend build passes
  </acceptance_criteria>
  <done>The content library UI is ready for browser verification against the live backend.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual verification of content blocks</name>
  <files>none (manual verification only)</files>
  <read_first>frontend/src/pages/content/HtmlTemplateCreatePage.tsx, frontend/src/pages/content/StaticPdfUploadPage.tsx, .planning/phases/03-content-building-blocks/03-VALIDATION.md</read_first>
  <what-built>
Phase 3 content blocks: schema-scoped HTML templates and locally stored static PDF assets.
  </what-built>
  <action>
Verify the full content library flow in the browser and confirm the phase meets CONTENT-01/02/03/VALID-01.
  </action>
  <how-to-verify>
1. Open the authenticated shell and navigate to the content library.
2. Create an HTML template scoped to an existing document type using only allowed tokens.
3. Confirm a template using an unknown token is rejected with a clear error.
4. Upload a static PDF and confirm it appears in the library.
5. Upload a PDF with a page range and confirm the stored asset reflects the extraction.
6. Open the stored asset and confirm the download/view path works.
  </how-to-verify>
  <verify>
    <automated>MANUAL — no automated command; explicit user confirmation required</automated>
  </verify>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
  <acceptance_criteria>
    - User confirms the content-library flow works end to end
    - Token validation failures are clear and blocking
    - PDF uploads are browsable and downloadable
  </acceptance_criteria>
  <done>User has confirmed that content blocks behave correctly in the browser.</done>
</task>

</tasks>

<verification>
`cd frontend && npm run build` must pass, and the manual browser checkpoint must be explicitly approved.
</verification>

<success_criteria>
- HTML templates are browsable and token-validated
- Static PDFs are uploadable and referenceable
- The content library is usable from the authenticated shell
</success_criteria>

