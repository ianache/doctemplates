---
phase: 03-content-building-blocks
plan: 01
type: tdd
wave: 1
depends_on: ["02-03"]
files_modified:
  - backend/pyproject.toml
  - backend/uv.lock
  - backend/app/config.py
  - backend/app/models/content_template.py
  - backend/app/models/__init__.py
  - backend/app/schemas/__init__.py
  - backend/app/schemas/content_template.py
  - backend/app/services/content_validation.py
  - backend/app/api/content_templates.py
  - backend/app/main.py
  - backend/alembic/env.py
  - backend/alembic/versions/0003_content_templates.py
  - backend/tests/test_content_templates.py
autonomous: true
requirements: [CONTENT-01, VALID-01]

must_haves:
  truths:
    - "An HTML template is scoped to a document type and stores raw HTML content"
    - "Saving a template with an unknown token is rejected with a clear 400-style error"
    - "Saving a template with only allowed tokens succeeds and preserves the HTML verbatim"
    - "Token validation uses the live document-type schema, not a hardcoded allowlist"
    - "Templates are browsable via list/detail endpoints behind existing auth"
  artifacts:
    - path: backend/app/models/content_template.py
      provides: "HtmlTemplate SQLAlchemy model and token validation support"
      contains: "class HtmlTemplate(Base)"
    - path: backend/app/schemas/content_template.py
      provides: "Pydantic request/response schemas for template authoring and browsing"
      contains: "class HtmlTemplateCreate"
    - path: backend/app/services/content_validation.py
      provides: "Token extraction and schema validation helpers"
      contains: "extract_template_tokens"
    - path: backend/app/api/content_templates.py
      provides: "POST/GET template endpoints"
      contains: "router = APIRouter(prefix=\"/api/content/templates\""
    - path: backend/alembic/versions/0003_content_templates.py
      provides: "Migration for template storage"
      contains: "html_templates"
    - path: backend/tests/test_content_templates.py
      provides: "Integration tests for allowed-token save, unknown-token rejection, auth gating, and detail browse"
  key_links:
    - from: backend/app/api/content_templates.py
      to: backend/app/services/content_validation.py
      via: "validate_template_tokens"
      pattern: "validate_template_tokens\\("
    - from: backend/app/api/content_templates.py
      to: backend/app/models/content_template.py
      via: "SQLAlchemy insert/query"
      pattern: "HtmlTemplate\\("
    - from: backend/app/api/content_templates.py
      to: backend/app/auth/dependencies.py
      via: "Depends(get_current_user)"
      pattern: "Depends\\(get_current_user\\)"
---

<objective>
Build the HTML-template half of Phase 3: persist raw HTML templates scoped to a document type, validate every token against the live document-type schema, and expose browsable template endpoints behind the Phase 1 auth gate.

Purpose: Satisfies `CONTENT-01` and `VALID-01` for the template side of the content library, so the frontend and the later PDF asset plan can build against a validated schema-scoped template contract.

Output: A tested `/api/content/templates` API with create/list/detail behavior and server-side token rejection for unknown fields.
</objective>

<execution_context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-content-building-blocks/03-CONTEXT.md
@.planning/phases/03-content-building-blocks/03-RESEARCH.md
@.planning/phases/03-content-building-blocks/03-VALIDATION.md
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Write failing tests for template authoring and validation</name>
  <files>backend/tests/test_content_templates.py</files>
  <read_first>backend/tests/conftest.py, backend/tests/test_document_types.py, backend/app/models/user.py, backend/app/models/session.py, backend/app/config.py</read_first>
  <behavior>
    - POSTing a valid template scoped to a real document type returns 201
    - POSTing a template with an unknown token returns 400 with a clear message
    - POSTing a template without auth returns 401
    - GET list returns lightweight rows with document-type name, token count, created-by, created-at
    - GET detail returns the raw HTML and token metadata
  </behavior>
  <action>
Create the test file first, then confirm the new content-template tests fail before implementation.
  </action>
  <verify>
    <automated>cd backend && VIRTUAL_ENV= uv run pytest tests/test_content_templates.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Template tests exist and cover allowed-token, rejected-token, auth, list, and detail behavior
    - The test command fails before implementation, proving the RED state
  </acceptance_criteria>
  <done>Template validation behavior is locked in by tests before any model/route work starts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Models, migration, validation helpers, and router wiring</name>
  <files>backend/pyproject.toml, backend/uv.lock, backend/app/config.py, backend/app/models/content_template.py, backend/app/models/__init__.py, backend/app/schemas/__init__.py, backend/app/schemas/content_template.py, backend/app/services/content_validation.py, backend/app/api/content_templates.py, backend/app/main.py, backend/alembic/env.py, backend/alembic/versions/0003_content_templates.py</files>
  <read_first>backend/app/models/document_type.py, backend/app/models/user.py, backend/app/models/session.py, backend/app/api/health.py, backend/alembic/env.py, backend/pyproject.toml</read_first>
  <action>
Implement the template model, token extraction/validation helper, migration, and `/api/content/templates` endpoints. Add the upload and parsing dependencies needed for future phase work only if they are required by the template implementation.
  </action>
  <verify>
    <automated>cd backend && VIRTUAL_ENV= uv run pytest tests/test_content_templates.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Unknown tokens are rejected with a clear 400-style error
    - Allowed tokens save cleanly
    - List/detail endpoints return the expected shapes
    - The template tests pass
  </acceptance_criteria>
  <done>Template storage and validation are live behind auth and backed by a migration.</done>
</task>

</tasks>

<verification>
`cd backend && VIRTUAL_ENV= uv run pytest tests/test_content_templates.py -q` must pass.
</verification>

<success_criteria>
- HTML templates are stored with document-type scope
- Unknown tokens are rejected before save
- Template list/detail endpoints work behind auth
</success_criteria>

