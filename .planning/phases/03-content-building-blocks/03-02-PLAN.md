---
phase: 03-content-building-blocks
plan: 02
type: tdd
wave: 2
depends_on: ["03-01"]
files_modified:
  - backend/pyproject.toml
  - backend/uv.lock
  - backend/app/config.py
  - backend/app/models/static_pdf_asset.py
  - backend/app/models/__init__.py
  - backend/app/schemas/static_pdf_asset.py
  - backend/app/services/content_storage.py
  - backend/app/api/static_pdfs.py
  - backend/app/main.py
  - backend/alembic/env.py
  - backend/alembic/versions/0004_static_pdf_assets.py
  - backend/tests/test_static_pdf_assets.py
autonomous: true
requirements: [CONTENT-02, CONTENT-03]

must_haves:
  truths:
    - "A PDF asset can be uploaded through the UI and stored by the platform"
    - "A PDF asset can optionally be stored as a selected page range from a larger PDF"
    - "Uploaded PDF assets are referenceable by stable IDs and browsable through the API"
    - "The stored file lives on local disk under a configurable content root"
    - "Upload/list/detail/download behavior is protected by existing auth"
  artifacts:
    - path: backend/app/models/static_pdf_asset.py
      provides: "StaticPdfAsset SQLAlchemy model and upload metadata"
      contains: "class StaticPdfAsset(Base)"
    - path: backend/app/schemas/static_pdf_asset.py
      provides: "Pydantic schemas for upload/list/detail metadata"
      contains: "class StaticPdfAssetCreate"
    - path: backend/app/services/content_storage.py
      provides: "Filesystem save/extract helpers for PDF assets"
      contains: "save_static_pdf_asset"
    - path: backend/app/api/static_pdfs.py
      provides: "POST/GET/download endpoints for PDF assets"
      contains: "router = APIRouter(prefix=\"/api/content/static-pdfs\""
    - path: backend/alembic/versions/0004_static_pdf_assets.py
      provides: "Migration for static PDF metadata"
      contains: "static_pdf_assets"
    - path: backend/tests/test_static_pdf_assets.py
      provides: "Integration tests for upload, extraction, list/detail, download, auth"
  key_links:
    - from: backend/app/api/static_pdfs.py
      to: backend/app/services/content_storage.py
      via: "save_static_pdf_asset"
      pattern: "save_static_pdf_asset\\("
    - from: backend/app/api/static_pdfs.py
      to: backend/app/models/static_pdf_asset.py
      via: "SQLAlchemy insert/query"
      pattern: "StaticPdfAsset\\("
    - from: backend/app/api/static_pdfs.py
      to: backend/app/auth/dependencies.py
      via: "Depends(get_current_user)"
      pattern: "Depends\\(get_current_user\\)"
---

<objective>
Build the static-PDF asset half of Phase 3: accept PDF uploads, optionally extract a page range, store the resulting file locally, and expose browsable/downloadable PDF-asset endpoints behind the Phase 1 auth gate.

Purpose: Satisfies `CONTENT-02` and `CONTENT-03` for the PDF asset side of the content library, so designs can reference stored assets later without needing the rendering pipeline yet.

Output: A tested `/api/content/static-pdfs` API with upload/list/detail/download behavior and local filesystem persistence.
</objective>

<execution_context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-content-building-blocks/03-CONTEXT.md
@.planning/phases/03-content-building-blocks/03-RESEARCH.md
@.planning/phases/03-content-building-blocks/03-VALIDATION.md
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Write failing tests for PDF upload and extraction</name>
  <files>backend/tests/test_static_pdf_assets.py</files>
  <read_first>backend/tests/conftest.py, backend/tests/test_document_types.py, backend/app/config.py</read_first>
  <behavior>
    - POSTing a valid PDF upload returns 201 and persists metadata
    - Uploading a page range stores the extracted subset and the expected page count
    - GET list returns lightweight rows with filename, page count, created-by, created-at
    - GET detail returns storage metadata and download URL information
    - GET download returns the stored PDF bytes with an application/pdf content type
    - POSTing without auth returns 401
  </behavior>
  <action>
Create the tests first and confirm they fail before implementation.
  </action>
  <verify>
    <automated>cd backend && VIRTUAL_ENV= uv run pytest tests/test_static_pdf_assets.py -q</automated>
  </verify>
  <acceptance_criteria>
    - PDF upload/extraction/list/detail/download tests exist
    - The test command fails before implementation, proving the RED state
  </acceptance_criteria>
  <done>PDF asset behavior is locked in by tests before model/storage work starts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Model, storage service, migration, and router wiring</name>
  <files>backend/pyproject.toml, backend/uv.lock, backend/app/config.py, backend/app/models/static_pdf_asset.py, backend/app/models/__init__.py, backend/app/schemas/static_pdf_asset.py, backend/app/services/content_storage.py, backend/app/api/static_pdfs.py, backend/app/main.py, backend/alembic/env.py, backend/alembic/versions/0004_static_pdf_assets.py</files>
  <read_first>backend/app/models/document_type.py, backend/app/models/user.py, backend/app/api/health.py, backend/alembic/env.py, backend/pyproject.toml</read_first>
  <action>
Implement the PDF asset model, filesystem storage helper, page-range extraction, migration, and upload/list/detail/download endpoints. Add the upload dependency required for multipart forms.
  </action>
  <verify>
    <automated>cd backend && VIRTUAL_ENV= uv run pytest tests/test_static_pdf_assets.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Uploaded PDF assets are persisted on local disk
    - Optional page-range extraction works
    - Download responses serve the stored PDF bytes
    - The PDF asset tests pass
  </acceptance_criteria>
  <done>Static PDF storage is live behind auth and backed by a migration.</done>
</task>

</tasks>

<verification>
`cd backend && VIRTUAL_ENV= uv run pytest tests/test_static_pdf_assets.py -q` must pass.
</verification>

<success_criteria>
- Static PDFs are uploaded, stored locally, and referenceable by stable IDs
- Optional page-range extraction works
- PDF asset upload/list/detail/download endpoints work behind auth
</success_criteria>

