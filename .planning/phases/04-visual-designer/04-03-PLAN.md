---
phase: 04-visual-designer
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/src/lib/documentDesigns.ts
  - frontend/src/pages/document-designs/DocumentDesignDetailPage.tsx
  - frontend/src/pages/document-designs/components/AddContentModal.tsx
  - frontend/src/pages/document-designs/components/DesignPageCard.tsx
  - frontend/src/pages/document-designs/components/DesignPageInspector.tsx
autonomous: false
requirements: [DESIGN-01, DESIGN-02]
must_haves:
  truths:
    - "The designer is a vertical ordered page stack"
    - "Users add templates and PDFs through separate modal actions"
    - "The stack supports drag-and-drop reorder and autosaves the resulting order"
    - "The selected page inspector edits title, notes, and page configuration"
    - "Cards show type-specific metadata: template tokens and PDF page count/range"
    - "Activation blocks invalid designs and surfaces template token incompatibility"
  artifacts:
    - path: frontend/src/pages/document-designs/DocumentDesignDetailPage.tsx
      provides: "Integrated designer screen with stack, modals, inspector, activation"
      contains: "DndContext"
    - path: frontend/src/pages/document-designs/components/DesignPageCard.tsx
      provides: "Sortable compact page card"
      contains: "useSortable"
    - path: frontend/src/pages/document-designs/components/AddContentModal.tsx
      provides: "Template/PDF content selector modal"
      contains: "Add Template"
    - path: frontend/src/pages/document-designs/components/DesignPageInspector.tsx
      provides: "Side inspector for selected page metadata"
      contains: "Notes"
---

<objective>
Build the interactive visual designer experience: add compatible content pages, inspect selected pages, drag-and-drop the ordered stack, autosave reorder, remove with local undo, and activate valid designs.

Purpose: Completes `DESIGN-01` and `DESIGN-02` in the browser while preserving Phase 4 boundaries: no final PDF rendering, no versioning, no external data resolution.

Output: A browser-verifiable designer UI backed by the Phase 4 API.
</objective>

<execution_context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-visual-designer/04-CONTEXT.md
@.planning/phases/04-visual-designer/04-RESEARCH.md
@frontend/src/pages/document-designs/DocumentDesignDetailPage.tsx
@frontend/src/lib/documentDesigns.ts
@frontend/src/lib/content.ts
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Add dnd-kit and sortable page stack</name>
  <files>frontend/package.json, frontend/package-lock.json, frontend/src/pages/document-designs/DocumentDesignDetailPage.tsx, frontend/src/pages/document-designs/components/DesignPageCard.tsx</files>
  <read_first>frontend/package.json, frontend/src/pages/document-designs/DocumentDesignDetailPage.tsx, .planning/phases/04-visual-designer/04-RESEARCH.md</read_first>
  <action>
Install `@dnd-kit/core`, `@dnd-kit/sortable`, and `@dnd-kit/utilities`. Implement a vertical `DndContext` + `SortableContext` stack where cards render in persisted order and call the reorder API after drag end.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Page stack uses dnd-kit sortable primitives
    - Reorder updates local order and calls the backend reorder endpoint
    - Cards remain compact and stable while dragging
    - Build passes
  </acceptance_criteria>
  <done>The design detail page has a draggable ordered stack with autosave reorder behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Content selection modals and page inspector</name>
  <files>frontend/src/pages/document-designs/DocumentDesignDetailPage.tsx, frontend/src/pages/document-designs/components/AddContentModal.tsx, frontend/src/pages/document-designs/components/DesignPageCard.tsx, frontend/src/pages/document-designs/components/DesignPageInspector.tsx, frontend/src/lib/documentDesigns.ts</files>
  <read_first>frontend/src/lib/content.ts, frontend/src/lib/documentDesigns.ts, frontend/src/pages/content/ContentLibraryPage.tsx, frontend/src/pages/content/HtmlTemplateDetailPage.tsx, frontend/src/pages/content/StaticPdfDetailPage.tsx</read_first>
  <action>
Implement separate Add Template and Add PDF modals, compatible content filtering, card type metadata, selected-page inspector for title/notes/config, page update calls, remove-with-undo behavior, and activation action/error display.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Template selector shows templates scoped to the design document type
    - PDF selector shows global PDFs plus document-type compatible PDFs
    - Inspector edits title, notes, and basic config for the selected page
    - PDF duplicate attempts surface a clear API error
    - Activation errors display invalid tokens when backend rejects compatibility
    - Build passes
  </acceptance_criteria>
  <done>The designer supports adding, inspecting, removing, and activating page stacks.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual browser verification of visual designer</name>
  <files>none (manual verification only)</files>
  <read_first>.planning/phases/04-visual-designer/04-CONTEXT.md, frontend/src/pages/document-designs/DocumentDesignDetailPage.tsx</read_first>
  <what-built>
Phase 4 visual designer: document design creation, compatible content selection, ordered page stack, drag-and-drop reorder, page inspector, and activation validation.
  </what-built>
  <action>
Verify the full Phase 4 flow in the browser.
  </action>
  <how-to-verify>
1. Open the authenticated shell and navigate to Document Designs.
2. Create a draft design scoped to a document type.
3. Add at least one compatible HTML template page.
4. Add at least one compatible/global static PDF page.
5. Confirm duplicate PDF add is rejected.
6. Drag pages into a new order and refresh; confirm order persists.
7. Select a page and edit title/notes in the inspector.
8. Activate a valid design.
9. Create or use an incompatible template case and confirm activation blocks with invalid token detail.
  </how-to-verify>
  <verify>
    <automated>MANUAL — no automated command; explicit user confirmation required</automated>
  </verify>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
  <acceptance_criteria>
    - User confirms document designs can be composed from templates and PDFs
    - User confirms drag-and-drop reorder persists
    - User confirms activation validation behaves as specified
  </acceptance_criteria>
  <done>User has confirmed the visual designer works end to end.</done>
</task>

</tasks>

<verification>
`cd frontend && npm run build` must pass, and the manual browser checkpoint must be explicitly approved.
</verification>

<success_criteria>
- Users can visually compose designs as ordered page stacks
- Users can add HTML template and static PDF pages
- Reorder persists and is visible after reload
- Inspector supports page-level title/notes/config edits
- Activation validation and errors are visible
</success_criteria>

