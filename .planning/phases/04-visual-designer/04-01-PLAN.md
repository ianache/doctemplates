---
phase: 04-visual-designer
plan: 01
type: tdd
wave: 1
depends_on: ["03-01", "03-02"]
files_modified:
  - backend/app/models/document_design.py
  - backend/app/models/static_pdf_asset.py
  - backend/app/models/__init__.py
  - backend/app/schemas/document_design.py
  - backend/app/schemas/static_pdf_asset.py
  - backend/app/services/design_validation.py
  - backend/app/api/document_designs.py
  - backend/app/api/static_pdfs.py
  - backend/app/main.py
  - backend/alembic/versions/0005_document_designs.py
  - backend/tests/test_document_designs.py
autonomous: true
requirements: [DESIGN-01, DESIGN-02]
must_haves:
  truths:
    - "A document design is scoped to exactly one document type"
    - "Draft designs can exist with no pages, but active designs require at least one page"
    - "Design pages preserve ordered positions and autosave reorder through an API"
    - "Template pages snapshot the referenced template and must match the design document type"
    - "PDF pages snapshot the referenced PDF and reject duplicates inside one design"
    - "PDF assets can be global or associated with a document type"
    - "Activation rejects incompatible template tokens with a clear 400-style error"
  artifacts:
    - path: backend/app/models/document_design.py
      provides: "DocumentDesign and DocumentDesignPage SQLAlchemy models"
      contains: "class DocumentDesign(Base)"
    - path: backend/app/schemas/document_design.py
      provides: "Pydantic schemas for create/list/detail/page operations"
      contains: "class DocumentDesignCreate"
    - path: backend/app/services/design_validation.py
      provides: "Design page snapshot and activation validation helpers"
      contains: "validate_design_activation"
    - path: backend/app/api/document_designs.py
      provides: "Authenticated document design endpoints"
      contains: "router = APIRouter(prefix=\"/api/document-designs\""
    - path: backend/alembic/versions/0005_document_designs.py
      provides: "Migration for designs, pages, and optional PDF document type association"
      contains: "document_designs"
    - path: backend/tests/test_document_designs.py
      provides: "Integration tests for design create/list/detail, add pages, reorder, activation validation, auth"
---

<objective>
Build the backend contract for Phase 4: document designs scoped to document types, ordered design pages referencing HTML templates or static PDFs, snapshot persistence, activation validation, and compatible content rules.

Purpose: This unlocks `DESIGN-01` and `DESIGN-02` at the API/data layer so the frontend designer can create a stable ordered page stack without implementing PDF generation or versioning.

Output: A tested `/api/document-designs` API plus optional document-type association for static PDF assets.
</objective>

<execution_context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-visual-designer/04-CONTEXT.md
@.planning/phases/04-visual-designer/04-RESEARCH.md
@.planning/phases/03-content-building-blocks/03-01-SUMMARY.md
@.planning/phases/03-content-building-blocks/03-02-SUMMARY.md
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Write failing design API tests</name>
  <files>backend/tests/test_document_designs.py</files>
  <read_first>backend/tests/conftest.py, backend/tests/test_content_templates.py, backend/tests/test_static_pdf_assets.py, backend/app/models/document_type.py, backend/app/models/content_template.py, backend/app/models/static_pdf_asset.py</read_first>
  <behavior>
    - Authenticated user can create a draft design scoped to a document type
    - Unauthenticated design requests return 401
    - User can add a compatible HTML template page and the page stores a snapshot
    - Adding a template from another document type returns 400
    - User can add a global static PDF page and a document-type associated static PDF page
    - Adding the same static PDF twice to one design returns 400
    - Reordering pages persists the new positions
    - Activating an empty design returns 400
    - Activating a design with invalid template tokens returns 400 and names the invalid tokens
    - Activating a valid design returns status `active`
  </behavior>
  <action>
Create `backend/tests/test_document_designs.py` first. Use the existing auth/session and content fixture patterns from Phase 3 tests. Confirm tests fail before implementation.
  </action>
  <verify>
    <automated>cd backend && VIRTUAL_ENV= uv run pytest tests/test_document_designs.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Tests cover create/list/detail, auth, template page compatibility, PDF page compatibility, duplicate PDF rejection, reorder persistence, and activation validation
    - The test command fails before model/router implementation
  </acceptance_criteria>
  <done>Backend design behavior is locked in by failing tests before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Implement models, migration, schemas, validation service, and routers</name>
  <files>backend/app/models/document_design.py, backend/app/models/static_pdf_asset.py, backend/app/models/__init__.py, backend/app/schemas/document_design.py, backend/app/schemas/static_pdf_asset.py, backend/app/services/design_validation.py, backend/app/api/document_designs.py, backend/app/api/static_pdfs.py, backend/app/main.py, backend/alembic/versions/0005_document_designs.py</files>
  <read_first>backend/app/api/document_types.py, backend/app/api/content_templates.py, backend/app/api/static_pdfs.py, backend/app/services/content_validation.py, backend/alembic/versions/0004_static_pdf_assets.py</read_first>
  <action>
Implement `DocumentDesign` and `DocumentDesignPage`, add optional `StaticPdfAsset.document_type_id`, create Pydantic schemas, register the new router, and implement helper functions for snapshots, content compatibility, reorder persistence, and activation validation.
  </action>
  <verify>
    <automated>cd backend && VIRTUAL_ENV= uv run pytest tests/test_document_designs.py tests/test_content_templates.py tests/test_static_pdf_assets.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Draft create/list/detail endpoints return expected shapes
    - Page add endpoints store complete snapshots
    - Reorder endpoint persists exact requested order
    - Activation enforces draft/active rules and invalid token reporting
    - Static PDF upload/list/detail remains backward-compatible for global PDFs
    - Existing Phase 3 content tests still pass
  </acceptance_criteria>
  <done>The backend design API is live behind auth and supports the Phase 4 designer UI.</done>
</task>

</tasks>

<verification>
`cd backend && VIRTUAL_ENV= uv run pytest tests/test_document_designs.py tests/test_content_templates.py tests/test_static_pdf_assets.py -q` must pass.
</verification>

<success_criteria>
- Design data model and endpoints support scoped designs and ordered pages
- Page snapshots are persisted for both templates and PDFs
- Reorder persists through API
- Activation blocks empty designs and incompatible template tokens
- Static PDFs support global or document-type associated compatibility
</success_criteria>

