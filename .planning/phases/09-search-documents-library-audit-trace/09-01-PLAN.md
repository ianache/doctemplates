---
phase: 09-search-documents-library-audit-trace
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/alembic/versions/0008_document_tracelogs.py
  - backend/app/models/document_tracelog.py
  - backend/app/models/document_issuance.py
  - backend/app/models/__init__.py
  - backend/app/schemas/document_issuance.py
  - backend/app/api/document_designs.py
  - backend/tests/test_document_tracelogs.py
autonomous: true
requirements:
  - SRCH-01
  - SRCH-03
must_haves:
  truths:
    - "Generated issuances create an auditable generation event"
    - "Each generated issuance stores an issuance-level Success/Failure status"
    - "Each issuance can own chronological tracelog rows"
    - "Deleting an issuance cascades its tracelog rows"
  artifacts:
    - path: "backend/app/models/document_tracelog.py"
      provides: "DocumentTracelog SQLAlchemy model per D-01"
      contains: "class DocumentTracelog"
    - path: "backend/app/models/document_issuance.py"
      provides: "DocumentIssuance status field and tracelog relationship"
      contains: "status"
    - path: "backend/alembic/versions/0008_document_tracelogs.py"
      provides: "document_tracelogs table migration"
      contains: "document_tracelogs"
    - path: "backend/app/schemas/document_issuance.py"
      provides: "tracelog response schema"
      contains: "DocumentTracelogOut"
  key_links:
    - from: "backend/app/models/document_issuance.py"
      to: "backend/app/models/document_tracelog.py"
      via: "SQLAlchemy relationship"
      pattern: "tracelogs.*relationship"
    - from: "backend/app/api/document_designs.py"
      to: "backend/app/models/document_tracelog.py"
      via: "generation event insert"
      pattern: "event_type=.*generation"
---

<objective>
Create the persistent audit trail foundation for generated document issuances.

Purpose: SRCH-03 and D-01 require a dedicated tracelog table that records generation, download, and share events without overloading issuance metadata.
Output: Migration, model, schema, relationship wiring, and generation-event logging with tests.
</objective>

<execution_context>
@D:/02-PERSONAL/01-PROJECTS/29-DocManagemet/.codex/get-shit-done/workflows/execute-plan.md
@D:/02-PERSONAL/01-PROJECTS/29-DocManagemet/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-search-documents-library-audit-trace/09-CONTEXT.md
@.planning/phases/09-search-documents-library-audit-trace/09-RESEARCH.md
@backend/app/models/document_issuance.py
@backend/app/models/document_design.py
@backend/app/models/user.py
@backend/app/api/document_designs.py
@backend/app/schemas/document_issuance.py
@backend/tests/test_generation_preview.py

<interfaces>
Existing `DocumentIssuance` has `id`, `design_version_id`, `file_path`, `user_id`, `input_data`, `created_at`, plus relationships `design_version` and `user`.
Existing `generate_document` in `backend/app/api/document_designs.py` creates `DocumentIssuance`, commits, refreshes, and returns it as `DocumentIssuanceOut`.
Existing backend tests authenticate by creating a user and setting `docmanagement_session` using `create_session`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add DocumentTracelog persistence contract</name>
  <files>backend/alembic/versions/0008_document_tracelogs.py, backend/app/models/document_tracelog.py, backend/app/models/document_issuance.py, backend/app/models/__init__.py, backend/app/schemas/document_issuance.py, backend/tests/test_document_tracelogs.py</files>
  <behavior>
    - Test 1: creating a DocumentTracelog with event_type generation for an issuance persists and can be queried chronologically.
    - Test 2: deleting a DocumentIssuance deletes its associated DocumentTracelog rows.
    - Test 3: invalid event_type values are rejected by the database/check constraint.
    - Test 4: DocumentIssuance requires an issuance-level `status` constrained to `success` or `failure`.
  </behavior>
  <action>Create `DocumentTracelog` exactly per D-01: UUID primary key defaulting uuid4, `issuance_id` FK to `document_issuances.id` with `ondelete="CASCADE"`, nullable `user_id` FK to `users.id` with `ondelete="SET NULL"`, `event_type` restricted to `generation`, `download`, `share`, JSON metadata column, and server-default `created_at`. Use SQLAlchemy 2 typed mappings matching the existing model style. In the same migration/model update, add `DocumentIssuance.status` as an issuance-level status constrained to exact values `success` and `failure`, defaulting existing/generated rows to `success`; this implements SRCH-01 status semantics and must not use parent design-version status. Add `DocumentIssuance.tracelogs` with `back_populates`, delete-orphan cascade, and chronological ordering. Export the model from `backend/app/models/__init__.py`. Add Alembic revision `0008` with `down_revision = "0007"` and idempotent table create/drop style matching `0007_document_issuances.py`; downgrade must remove `document_tracelogs` and the `document_issuances.status` addition/check constraint. Add `DocumentTracelogOut` schema with `id`, `issuance_id`, `event_type`, nullable `user_id`, `metadata`, and `created_at`, and include issuance `status` in the issuance schemas. Use field aliasing if needed so the public schema exposes `metadata` while avoiding SQLAlchemy reserved-name conflicts in the model.</action>
  <verify>
    <automated>cd backend; rtk pytest tests/test_document_tracelogs.py -x</automated>
  </verify>
  <done>`DocumentTracelog` exists per D-01, `DocumentIssuance.status` exists as `success`/`failure`, migrations create/drop `document_tracelogs` and issuance status changes, and tests prove persistence, cascade delete, event-type enforcement, and status constraint enforcement.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Log generation events when issuances are created</name>
  <files>backend/app/api/document_designs.py, backend/tests/test_document_tracelogs.py</files>
  <behavior>
    - Test 1: successful `POST /api/document-designs/{design_id}/generate` creates one `generation` tracelog row for the returned issuance.
    - Test 2: the generation row has the authenticated user's id and metadata containing at least the source endpoint or action context.
  </behavior>
  <action>After creating a `DocumentIssuance` in `generate_document`, set issuance `status="success"` only after the PDF file has been generated and the issuance row is ready to persist, then insert a `DocumentTracelog` with `event_type="generation"`, `user_id=user.id`, and metadata identifying the generation route/design id. If the generation path catches and persists failed issuance attempts anywhere in the existing code, set those rows to `status="failure"`; otherwise failed requests may remain non-persisted but the schema/API must still support filtering seeded or future `failure` rows. Keep successful issuance and tracelog creation in the same transaction so a success response never returns without its initial audit row. Do not add expiration/link UX here; expiration is deferred by context.</action>
  <verify>
    <automated>cd backend; rtk pytest tests/test_document_tracelogs.py tests/test_generation_preview.py::test_generate -x</automated>
  </verify>
  <done>Every persisted issuance returned by the generation endpoint has a corresponding generation tracelog entry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API route -> database | Authenticated user actions create audit rows that must not be forgeable by client payloads. |
| Issuance deletion -> audit deletion | Cascading behavior removes audit rows only when the owning issuance is deleted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | `DocumentTracelog.event_type` | mitigate | Enforce exact event values with SQLAlchemy/Alembic check constraint. |
| T-09-02 | Repudiation | `generate_document` audit insert | mitigate | Create `generation` row server-side in the same transaction as issuance creation; never accept event_type/user_id from the request body. |
| T-09-SC | Tampering | package installs | accept | No package-manager installs are planned. |
</threat_model>

<verification>
Run `cd backend; rtk pytest tests/test_document_tracelogs.py tests/test_generation_preview.py -x`.
</verification>

<success_criteria>
SRCH-03 backend persistence foundation is complete, and SRCH-01 status semantics are available: the database stores issuance-level `success`/`failure` status plus chronological tracelog events, generation is logged automatically, and the model/schema are available for API listing in the next plan.
</success_criteria>

<output>
Create `.planning/phases/09-search-documents-library-audit-trace/09-01-SUMMARY.md` when done.
</output>
