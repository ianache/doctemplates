---
phase: 06-generation-preview-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pyproject.toml
  - backend/app/config.py
  - backend/app/models/__init__.py
  - backend/app/models/document_issuance.py
  - backend/alembic/versions/0007_document_issuances.py
  - backend/app/services/pdf_generator.py
autonomous: true
requirements: [GEN-01, GEN-02]
must_haves:
  truths:
    - "A database table named document_issuances tracks issued production PDFs"
    - "Jinja2 dynamically interpolates token variables in templates"
    - "xhtml2pdf compiles HTML and CSS into PDF bytes"
    - "pypdf merges HTML dynamic pages and static PDF files in sequence"
  artifacts:
    - path: "backend/pyproject.toml"
      provides: "xhtml2pdf and Jinja2 package dependencies"
    - path: "backend/app/config.py"
      provides: "issuance_storage_root directory configuration"
    - path: "backend/app/models/document_issuance.py"
      provides: "DocumentIssuance SQLAlchemy model"
    - path: "backend/alembic/versions/0007_document_issuances.py"
      provides: "Alembic migration for document_issuances table"
    - path: "backend/app/services/pdf_generator.py"
      provides: "Helper functions to validate/coerce inputs, expand flat dictionary keys, render templates, and compose final merged PDFs"
---

<objective>
Configure Phase 6 dependencies, storage settings, and the database tracking schema, and implement the core PDF generator rendering pipeline (`pdf_generator.py` service) including type-safe payload validation/coercion, nested dictionary expansion, HTML-to-PDF compilation via `xhtml2pdf`, and multi-page PDF merging via `pypdf`.
</objective>

<execution_context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-generation-preview-api/06-CONTEXT.md
@.planning/phases/06-generation-preview-api/06-RESEARCH.md
@.planning/phases/06-generation-preview-api/06-VALIDATION.md
@backend/pyproject.toml
@backend/app/config.py
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-generation-preview-api/06-CONTEXT.md
@.planning/phases/06-generation-preview-api/06-RESEARCH.md
@.planning/phases/06-generation-preview-api/06-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Configure dependencies, storage settings, and migration for document issuances</name>
  <files>backend/pyproject.toml, backend/app/config.py, backend/app/models/__init__.py, backend/app/models/document_issuance.py, backend/alembic/versions/0007_document_issuances.py</files>
  <read_first>backend/pyproject.toml, backend/app/config.py, backend/app/models/__init__.py, backend/alembic/versions/0006_document_design_versioning.py</read_first>
  <action>
    Add xhtml2pdf and Jinja2 dependencies to backend/pyproject.toml (per D-01, D-02). Add issuance_storage_root configuration value to Settings class in backend/app/config.py (per D-09). Create backend/app/models/document_issuance.py defining the DocumentIssuance SQLAlchemy model with columns: id, design_version_id, file_path, user_id, input_data, created_at, using RESTRICT constraint on design deletion to prevent accidental audit record deletion (per D-10, D-12). Register DocumentIssuance in backend/app/models/__init__.py. Create Alembic migration script backend/alembic/versions/0007_document_issuances.py. Execute backend database migration.
  </action>
  <verify>
    <automated>cd backend && uv run alembic upgrade head</automated>
  </verify>
  <acceptance_criteria>
    - backend/pyproject.toml contains dependencies xhtml2pdf and Jinja2.
    - backend/app/config.py exposes issuance_storage_root setting.
    - postgres table document_issuances is created successfully with columns: id, design_version_id, file_path, user_id, input_data, created_at.
  </acceptance_criteria>
  <done>Phase 6 dependencies are installed, storage configuration is updated, and the DocumentIssuance model and database table are created.</done>
</task>

<task type="auto">
  <name>Implement core PDF composition, payload validation, coercion, and rendering pipeline</name>
  <files>backend/app/services/pdf_generator.py</files>
  <read_first>backend/app/models/document_type.py, backend/app/services/content_storage.py, backend/app/services/content_validation.py, backend/app/services/design_validation.py</read_first>
  <action>
    Implement validation, coercion, nested dictionary expansion, HTML template rendering, and page merging inside backend/app/services/pdf_generator.py. Create validate_and_coerce_payload helper to check for missing fields (per D-05) and generate mock values if fallback is enabled (per D-13), or coerce matching fields (per D-07, D-08) and ignore extra ones (per D-06). Write expand_flat_dict to convert dot-notation payload keys into nested dictionary structures. Create render_html_page_to_pdf to render Jinja2 templates using standard fonts (per D-04) and a base stylesheet (per D-03), generating PDF bytes via xhtml2pdf (per D-01). Implement generate_composed_pdf to validate the payload, expand the dictionary, render HTML templates, read static assets, and merge all blocks using pypdf.
  </action>
  <verify>
    <automated>cd backend && uv run python -c "import app.services.pdf_generator; print('Service loaded successfully')"</automated>
  </verify>
  <acceptance_criteria>
    - pdf_generator.py implements validate_and_coerce_payload, expand_flat_dict, render_html_page_to_pdf, and generate_composed_pdf.
    - Missing schema fields trigger validation errors (or get auto-generated mocks if requested).
    - Date format validation strictly checks for ISO 8601 formatting.
    - HTML and static PDF layers are correctly processed and merged.
  </acceptance_criteria>
  <done>Core PDF composition service with data validation, coercion, HTML rendering, and static merging is successfully implemented.</done>
</task>

</tasks>

<threat_model>
### Trust Boundaries Map
1. Untrusted Client <---[HTTP Requests with Auth JWT/Cookie]---> FastAPI backend (Routers)
2. FastAPI backend <---[SQLAlchemy ORM / SQL]---> Postgres Database (issuances, user logs)
3. FastAPI backend <---[Python standard library Path/write]---> Local Storage (.content-storage/issuances)

### STRIDE Threat Register
| Threat ID | Category | Description | Mitigation Plan |
|---|---|---|---|
| T-01 | Spoofing | Client issues documents under another user's identity or access unauthorized designs. | Gated by `get_current_user` dependency in FastAPI router. Active user ID (`user.id`) is stored in `DocumentIssuance.user_id`. |
| T-02 | Tampering | User tries to access files outside the storage root using path traversal attacks. | Strict UUID validation on issuance IDs. Use Python `Path` relative to the configured storage root to avoid path traversal. |
| T-03 | Repudiation | Operational user claims they didn't generate a document, or backend fails to audit generated legal PDFs. | Every generation logs the input payload (`input_data`), active user (`user_id`), creation timestamp, and version in `DocumentIssuance`. |
| T-04 | Info Disclosure | Unauthorized user downloads generated PDF issuances of other users. | Download API `/api/issuances/{issuance_id}/download` is authenticated and restricted. |
| T-05 | DoS | Malicious payloads or infinite loops in templates exhaust system resources. | Payload is strictly schema-validated and coerced to prevent malformed data. Jinja2 templates are strictly validated at activation time. Standard typography is restricted to Helvetica, Times, Courier. |
| T-06 | Privilege Elevation | Jinja template injection executing arbitrary Python commands. | Use Jinja2 environment in a sandboxed mode or simple parsing (standard Environment sandbox/no-extensions) to ensure only predefined date formatting filters and variables are executed. |
</threat_model>

<verification>
Verify database schema and import capability:
1. Run alembic upgrade:
   `cd backend && uv run alembic upgrade head`
2. Verify pdf_generator imports successfully:
   `cd backend && uv run python -c "import app.services.pdf_generator"`
</verification>

<success_criteria>
- document_issuances table successfully created and registered
- pdf_generator service compiles and imports without issues
</success_criteria>
