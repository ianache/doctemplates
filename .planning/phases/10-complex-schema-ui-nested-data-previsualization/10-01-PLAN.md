---
phase: 10-complex-schema-ui-nested-data-previsualization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/schemaFields.ts
  - frontend/src/pages/document-types/components/SchemaFieldEditor.tsx
  - frontend/src/pages/document-types/DocumentTypeCreatePage.tsx
  - frontend/src/pages/document-types/DocumentTypeDetailPage.tsx
autonomous: true
requirements:
  - COMPUI-01
must_haves:
  truths:
    - "User can add nested object schema leaves such as cliente.direccion.calle from the Document Type create form."
    - "User can add object-list schema leaves such as cliente.contactos[].nombre without changing the backend fields[] contract."
    - "User can view existing document type fields grouped as an inferred nested/list tree."
  artifacts:
    - path: "frontend/src/lib/schemaFields.ts"
      provides: "Pure flat field path parsing, tree grouping, path validation, case-insensitive duplicate checks, and field normalization helpers"
      exports: ["buildSchemaFieldTree", "validateSchemaFields", "normalizeSchemaFields"]
    - path: "frontend/src/pages/document-types/components/SchemaFieldEditor.tsx"
      provides: "Visual nested/list schema field editor over react-hook-form fields[] rows"
      exports: ["SchemaFieldEditor"]
    - path: "frontend/src/pages/document-types/DocumentTypeCreatePage.tsx"
      provides: "Document Type create form using SchemaFieldEditor while submitting DocumentTypeFieldIn[]"
    - path: "frontend/src/pages/document-types/DocumentTypeDetailPage.tsx"
      provides: "Nested/list tree visualization for existing document type fields"
  key_links:
    - from: "frontend/src/pages/document-types/DocumentTypeCreatePage.tsx"
      to: "frontend/src/lib/documentTypes.ts"
      via: "createDocumentType payload fields"
      pattern: "fields: values.fields.map"
    - from: "frontend/src/pages/document-types/DocumentTypeDetailPage.tsx"
      to: "frontend/src/lib/schemaFields.ts"
      via: "buildSchemaFieldTree(documentType.fields)"
      pattern: "buildSchemaFieldTree"
---

<objective>
Build the Document Types UI support for complex schema paths.

Purpose: Satisfy COMPUI-01 by making nested object and object-list schema fields visible and authorable without changing the backend flat field path API created in Phase 7.
Output: A reusable schema field adapter, a visual create-form editor, and a detail-page tree view.
</objective>

<execution_context>
@D:/02-PERSONAL/01-PROJECTS/29-DocManagemet/.codex/get-shit-done/workflows/execute-plan.md
@D:/02-PERSONAL/01-PROJECTS/29-DocManagemet/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-complex-schema-ui-nested-data-previsualization/10-RESEARCH.md
@.planning/phases/07-backend-core-nested-data-case-insensitive-matching/07-01-SUMMARY.md
@frontend/src/lib/documentTypes.ts
@frontend/src/pages/document-types/DocumentTypeCreatePage.tsx
@frontend/src/pages/document-types/DocumentTypeDetailPage.tsx
@.planning/phases/10-complex-schema-ui-nested-data-previsualization/10-PATTERNS.md

<interfaces>
From frontend/src/lib/documentTypes.ts:
- FieldType = "string" | "number" | "date" | "boolean"
- DocumentTypeFieldIn has name, type, description
- DocumentTypeDetail has fields: DocumentTypeField[]
- createDocumentType(payload: DocumentTypeCreatePayload): Promise<DocumentTypeDetail>

Backend contract from Phase 7:
- Nested paths remain flat strings in fields[].
- Object children use dot notation, e.g. cliente.direccion.calle.
- Object-list children use [] on the list segment, e.g. cliente.contactos[].nombre.
- Backend remains authoritative for structural conflicts and path validation.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add schema field path adapter</name>
  <files>frontend/src/lib/schemaFields.ts</files>
  <behavior>
    - buildSchemaFieldTree groups cliente.direccion.calle under object nodes cliente and direccion with a primitive calle leaf.
    - buildSchemaFieldTree treats cliente.contactos[].nombre as cliente object -> contactos object-list -> nombre primitive leaf and never emits a contactos[] JSON key.
    - validateSchemaFields rejects empty names, malformed paths, and case-insensitive duplicates such as Cliente.Nombre plus cliente.nombre before submit.
    - normalizeSchemaFields trims names/descriptions and preserves FieldType values for the existing backend fields[] payload.
  </behavior>
  <action>Create a pure TypeScript helper module for schema path handling. Import FieldType and DocumentTypeFieldIn as type-only imports from documentTypes. Define explicit node types for object, object-list, and primitive leaves. Mirror backend path semantics lightly for UX only: path segments are dot-delimited, list segments end in [], primitive type applies only to leaves, and duplicate comparison is case-insensitive. Do not add dependencies and do not introduce a nested API payload format.</action>
  <verify>
    <automated>rtk proxy powershell -NoProfile -Command "Set-Location frontend; npm run build"</automated>
  </verify>
  <done>schemaFields.ts exports the tree builder, validation result, and normalization helpers; frontend build passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace primitive rows with visual schema editor</name>
  <files>frontend/src/pages/document-types/components/SchemaFieldEditor.tsx, frontend/src/pages/document-types/DocumentTypeCreatePage.tsx</files>
  <behavior>
    - User can add primitive leaves, nested object leaves, and object-list leaves from the create form.
    - The submitted payload remains DocumentTypeFieldIn[] with flat names.
    - The form blocks case-insensitive duplicate names before calling createDocumentType.
    - Backend validation failures still surface as a form-level error.
  </behavior>
  <action>Create SchemaFieldEditor (D-01) as a controlled component around the existing react-hook-form/useFieldArray data shape. It may expose path-building controls plus the resulting field path, but it must keep values.fields as the source of truth and must not store a separate nested schema. Update DocumentTypeCreatePage to use SchemaFieldEditor (D-01), call validateSchemaFields/normalizeSchemaFields on submit, and pass normalized fields to createDocumentType. Keep the current route, create action, and navigation behavior.</action>
  <verify>
    <automated>rtk proxy powershell -NoProfile -Command "Set-Location frontend; npm run build"</automated>
  </verify>
  <done>Create form supports authoring flat, nested object, and object-list field names with clear validation and unchanged backend payload shape.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Render document type fields as inferred tree</name>
  <files>frontend/src/pages/document-types/DocumentTypeDetailPage.tsx</files>
  <behavior>
    - Existing flat fields render as a readable hierarchy.
    - Object-list nodes are visually distinguished from object nodes.
    - Primitive leaves still show field type, full path, and description.
  </behavior>
  <action>Use buildSchemaFieldTree in DocumentTypeDetailPage (D-02) and replace the flat field list with a compact nested/list tree. Preserve existing metadata display and empty/not-found states. Keep the full backend path visible on leaf rows so users understand the canonical token name.</action>
  <verify>
    <automated>rtk proxy powershell -NoProfile -Command "Set-Location frontend; npm run build"</automated>
  </verify>
  <done>Document type detail pages show complex schemas as a hierarchy while preserving canonical field path visibility.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> Document Type API | User-authored schema paths cross into backend validation. |
| UI helper -> Backend contract | Client-side path validation is advisory and must not replace backend Pydantic validation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | SchemaFieldEditor | mitigate | Perform case-insensitive duplicate checks and preserve backend submission to createDocumentType so backend validators remain authoritative. |
| T-10-02 | Information Disclosure | DocumentTypeDetailPage | accept | Detail page displays schema metadata already returned to authenticated users by existing endpoint. |
| T-10-SC | Tampering | npm installs | mitigate | No package installs are planned; keep package-lock unchanged. |
</threat_model>

<verification>
Run frontend build after each task. Confirm no backend files or API schema files were modified.
</verification>

<success_criteria>
COMPUI-01 is covered when users can create nested object/list field paths in the Document Type UI, the backend still receives flat fields[], duplicate/malformed paths are caught before submit where possible, and existing document type schemas render as nested/list trees.
</success_criteria>

<output>
Create `.planning/phases/10-complex-schema-ui-nested-data-previsualization/10-01-SUMMARY.md` when done.
</output>
