---
phase: 10-complex-schema-ui-nested-data-previsualization
plan: 03
type: execute
wave: 3
depends_on:
  - 10-01
  - 10-02
files_modified:
  - frontend/src/pages/document-designs/DocumentDesignDetailPage.tsx
autonomous: true
requirements:
  - COMPUI-02
must_haves:
  truths:
    - "Designer users can open a generated preview mode from the existing design detail page."
    - "Designer users can reset mock data from the design's document type fields, edit the nested JSON, and request a PDF preview."
    - "Preview requests use POST /api/document-designs/{id}/preview and do not create issuances."
  artifacts:
    - path: "frontend/src/pages/document-designs/DocumentDesignDetailPage.tsx"
      provides: "Integrated nested data preview panel and generated PDF preview mode"
  key_links:
    - from: "frontend/src/pages/document-designs/DocumentDesignDetailPage.tsx"
      to: "frontend/src/lib/documentTypes.ts"
      via: "getDocumentType(design.document_type_id)"
      pattern: "getDocumentType"
    - from: "frontend/src/pages/document-designs/DocumentDesignDetailPage.tsx"
      to: "frontend/src/lib/documentDesigns.ts"
      via: "previewDocumentDesign(design.id, payload)"
      pattern: "previewDocumentDesign"
---

<objective>
Wire nested mock data previsualization into the document designer.

Purpose: Complete COMPUI-02 by making backend-generated PDF preview available inside the existing visual editor with editable nested/array mock data.
Output: Designer detail page integration using the components and helpers from plans 10-01 and 10-02.
</objective>

<execution_context>
@D:/02-PERSONAL/01-PROJECTS/29-DocManagemet/.codex/get-shit-done/workflows/execute-plan.md
@D:/02-PERSONAL/01-PROJECTS/29-DocManagemet/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-complex-schema-ui-nested-data-previsualization/10-RESEARCH.md
@.planning/phases/09-search-documents-library-audit-trace/09-03-SUMMARY.md
@frontend/src/pages/document-designs/DocumentDesignDetailPage.tsx
@frontend/src/lib/documentDesigns.ts
@frontend/src/lib/documentTypes.ts
@frontend/src/lib/schemaFields.ts
@frontend/src/pages/document-designs/components/MockDataPanel.tsx
@frontend/src/pages/document-designs/components/PreviewFrame.tsx
@.planning/phases/10-complex-schema-ui-nested-data-previsualization/10-PATTERNS.md

<interfaces>
Phase 9 risk:
- Phase 09 frontend build passed, but live browser verification is human_needed. Use its code patterns, but do not assume Phase 9 UAT passed. If Phase 9 UAT changes shared shell/navigation behavior, resolve that before final Phase 10 UAT.

Designer current behavior:
- DocumentDesignDetailPage owns design loading, selected page preview, right-side inspector, and draft/active actions.
- Current center preview is selected-page srcDoc/static placeholder; Phase 10 adds generated whole-design PDF preview without removing selected-page inspection.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Load schema fields and initialize mock data in designer</name>
  <files>frontend/src/pages/document-designs/DocumentDesignDetailPage.tsx</files>
  <behavior>
    - When a design loads, the page loads its document type fields using design.document_type_id.
    - Mock JSON initializes from generateMockDataFromFields(fields).
    - Reset restores generated mock JSON from the current schema.
    - Failure to load schema fields shows a non-blocking preview error and does not break fragment editing.
  </behavior>
  <action>Import getDocumentType, generateMockDataFromFields, and MockDataPanel (D-03) dependencies. Add state for document type fields, mock JSON text, parsed payload, parse error, preview loading/error, and generated PDF blob. Load document type detail after design load by document_type_id. Keep selected-page preview and inspector behavior intact. Do not add backend calls beyond existing document type detail and preview client calls.</action>
  <verify>
    <automated>rtk proxy powershell -NoProfile -Command "Set-Location frontend; npm run build"</automated>
  </verify>
  <done>Designer initializes editable nested mock JSON from the design's document type schema without disrupting existing designer actions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add generated PDF preview mode</name>
  <files>frontend/src/pages/document-designs/DocumentDesignDetailPage.tsx</files>
  <behavior>
    - User can switch between selected-page preview and generated PDF preview.
    - Clicking preview posts parsed mock JSON through previewDocumentDesign.
    - Successful preview renders the returned PDF in PreviewFrame.
    - Preview errors and JSON parse errors are shown next to preview controls.
  </behavior>
  <action>Integrate MockDataPanel (D-03) in the designer side panel or an adjacent preview control area without removing DesignPageInspector. Add a generated preview toggle/control in the center preview header (D-04). On preview action, parse the JSON text, require a JSON object root, call previewDocumentDesign(design.id, payload), and pass the returned Blob to PreviewFrame (D-04). Keep /generate unused and do not persist mock payloads. Make the layout responsive and avoid nested cards inside cards.</action>
  <verify>
    <automated>rtk proxy powershell -NoProfile -Command "Set-Location frontend; npm run build"</automated>
  </verify>
  <done>DocumentDesignDetailPage provides an interactive nested/array mock data editor and backend-generated PDF preview in the visual editor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Designer UI -> Document Type API | Design document_type_id is used to fetch schema fields for preview setup. |
| Designer UI -> Preview API | User-edited mock data is sent to backend PDF preview. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06 | Tampering | DocumentDesignDetailPage mock payload | mitigate | Require parsed JSON object root client-side and rely on backend validate/coerce for schema enforcement. |
| T-10-07 | Repudiation | Preview action | accept | Preview endpoint intentionally does not persist issuance or audit rows; Phase 6 tests cover no-persistence behavior. |
| T-10-08 | Denial of Service | Preview action | mitigate | Disable preview button while request is in flight and display backend errors instead of retry loops. |
| T-10-SC | Tampering | npm installs | mitigate | No package installs are planned; keep package-lock unchanged. |
</threat_model>

<verification>
Run frontend build. Recommended phase-level backend contract check remains `rtk proxy powershell -NoProfile -Command "Set-Location backend; uv run pytest tests/test_generation_preview.py tests/test_nested_case_insensitive.py -q"` because COMPUI-02 relies on the existing preview endpoint.
</verification>

<success_criteria>
COMPUI-02 is functionally implemented when the visual editor can load schema-derived nested/array mock JSON, let users edit it, call the existing preview endpoint, and render the returned PDF without creating issuances.
</success_criteria>

<output>
Create `.planning/phases/10-complex-schema-ui-nested-data-previsualization/10-03-SUMMARY.md` when done.
</output>
