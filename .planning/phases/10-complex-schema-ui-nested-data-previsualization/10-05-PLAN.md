---
phase: 10-complex-schema-ui-nested-data-previsualization
plan: 05
type: execute
wave: 4
depends_on:
  - 10-03
files_modified:
  - frontend/src/pages/document-designs/DocumentDesignDetailPage.tsx
autonomous: true
requirements:
  - COMPUI-02
must_haves:
  truths:
    - "The user-edited custom mock JSON payload is saved to localStorage whenever it changes."
    - "When loading a design, the designer attempts to load a saved mock JSON payload from localStorage before falling back to generating new mock data."
  artifacts:
    - path: "frontend/src/pages/document-designs/DocumentDesignDetailPage.tsx"
      provides: "localStorage persistence of custom mock JSON keyed by design ID"
  key_links:
    - from: "frontend/src/pages/document-designs/DocumentDesignDetailPage.tsx"
      to: "localStorage"
      via: "localStorage.getItem and localStorage.setItem"
      pattern: "localStorage\\.getItem\\(`mock_payload_"
---

<objective>
Implement local browser storage persistence for the custom mock JSON payload.

Purpose: Satisfy D-05 of COMPUI-02 by persisting the user's custom mock JSON payload locally in the browser's localStorage, keyed by design ID, so that changes are preserved across page reloads.
Output: Integrated localStorage reading and writing in DocumentDesignDetailPage.
</objective>

<execution_context>
@D:/02-PERSONAL/01-PROJECTS/29-DocManagemet/.codex/get-shit-done/workflows/execute-plan.md
@D:/02-PERSONAL/01-PROJECTS/29-DocManagemet/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-complex-schema-ui-nested-data-previsualization/10-CONTEXT.md
@.planning/phases/10-complex-schema-ui-nested-data-previsualization/10-PATTERNS.md
@frontend/src/pages/document-designs/DocumentDesignDetailPage.tsx
@.planning/phases/10-complex-schema-ui-nested-data-previsualization/10-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add localStorage persistence for mock JSON</name>
  <files>frontend/src/pages/document-designs/DocumentDesignDetailPage.tsx</files>
  <behavior>
    - When loading a design, check localStorage for a saved mock payload using key `mock_payload_${designId}` per D-05.
    - If a valid JSON payload is found in localStorage, use it as the initial mock JSON state.
    - If no payload is found, or if parsing fails, fallback to generating the mock payload from schema fields.
    - Whenever the user successfully edits or updates the mock JSON payload in the designer, save the new payload to localStorage under key `mock_payload_${designId}` per D-05.
  </behavior>
  <action>Modify DocumentDesignDetailPage.tsx to check localStorage (D-05) inside the mock JSON initialization state or an effect. Add an updater function or effect that sets `localStorage.setItem('mock_payload_' + designId, JSON.stringify(payload))` (D-05) whenever the parsed mock JSON payload changes. Ensure that reset action clears or updates the localStorage item appropriately to align with the schema-generated fallback.</action>
  <verify>
    <automated>rtk proxy powershell -NoProfile -Command "Set-Location frontend; npm run build"</automated>
  </verify>
  <done>Custom mock JSON payload is persisted to browser localStorage keyed by design ID, and is loaded correctly on page refresh.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser localStorage -> React State | JSON string loaded from localStorage is parsed into application memory. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-11 | Tampering | localStorage payload | mitigate | Wrap localStorage reading and parsing in try-catch blocks, and fall back safely to schema-generated mock data if parsing fails. |
| T-10-12 | Information Disclosure | localStorage data | accept | The mock data contains sample values rather than sensitive production user data. |
| T-10-SC | Tampering | npm installs | mitigate | No package installs are planned; keep package-lock unchanged. |
</threat_model>

<verification>
Run frontend build to ensure there are no TypeScript or compilation errors.
</verification>

<success_criteria>
COMPUI-02 is fully implemented when mock data changes persist in localStorage across browser reloads.
</success_criteria>

<output>
Create `.planning/phases/10-complex-schema-ui-nested-data-previsualization/10-05-SUMMARY.md` when done.
</output>
