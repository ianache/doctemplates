---
phase: 10-complex-schema-ui-nested-data-previsualization
plan: 04
type: execute
wave: 5
depends_on:
  - 10-05
files_modified: []
autonomous: false
requirements:
  - COMPUI-01
  - COMPUI-02
must_haves:
  truths:
    - "A human can create and view nested/list document type schemas in the browser."
    - "A human can previsualize a design with nested/array mock data in the browser."
    - "Phase 9 human_needed status is recorded as a risk, not treated as a prerequisite for Phase 10 implementation."
  artifacts: []
  key_links:
    - from: "Document Type create/detail UI"
      to: "backend /api/document-types"
      via: "flat fields[] payload"
      pattern: "cliente.contactos\\[\\]\\.nombre"
    - from: "Document Design detail UI"
      to: "backend /api/document-designs/{id}/preview"
      via: "previewDocumentDesign"
      pattern: "application/pdf"
---

<objective>
Verify Phase 10 behavior in a real browser session.

Purpose: Confirm COMPUI-01 and COMPUI-02 work end-to-end with authenticated app state, PDF rendering, and Phase 9 navigation risk visible to the user.
Output: Human verification result and Phase 10 summary.
</objective>

<execution_context>
@D:/02-PERSONAL/01-PROJECTS/29-DocManagemet/.codex/get-shit-done/workflows/execute-plan.md
@D:/02-PERSONAL/01-PROJECTS/29-DocManagemet/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-complex-schema-ui-nested-data-previsualization/10-RESEARCH.md
@.planning/phases/09-search-documents-library-audit-trace/09-VERIFICATION.md
@.planning/phases/10-complex-schema-ui-nested-data-previsualization/10-01-SUMMARY.md
@.planning/phases/10-complex-schema-ui-nested-data-previsualization/10-02-SUMMARY.md
@.planning/phases/10-complex-schema-ui-nested-data-previsualization/10-03-SUMMARY.md
@.planning/phases/10-complex-schema-ui-nested-data-previsualization/10-05-SUMMARY.md

<interfaces>
Phase 9 risk:
- Phase 09 verification is human_needed. Do not require Phase 10 execution before Phase 09 UAT. If Phase 9 UAT later changes shared shell/navigation behavior, rerun this checkpoint after resolving the shared UI issue.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated phase checks</name>
  <files></files>
  <action>Run the frontend production build and focused backend tests that protect nested preview behavior. If backend tests cannot run due to environment or dependency availability, record the exact failure in the summary and proceed to the human checkpoint only after frontend build passes.</action>
  <verify>
    <automated>rtk proxy powershell -NoProfile -Command "Set-Location frontend; npm run build"</automated>
    <automated>rtk proxy powershell -NoProfile -Command "Set-Location backend; uv run pytest tests/test_generation_preview.py tests/test_nested_case_insensitive.py -q"</automated>
  </verify>
  <done>Frontend build passes and backend preview/nested tests pass or any environment blocker is documented.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Browser UAT for complex schema UI and nested data preview</name>
  <files></files>
  <action>Pause execution after automated checks and ask the user to perform the browser workflow below. Do not mark Phase 10 complete until the user approves or issues are fixed in a follow-up gap plan.</action>
  <what-built>Phase 10 Document Types complex schema editor/detail tree and Document Designer nested mock-data PDF preview.</what-built>
  <how-to-verify>
    1. Start the existing backend/frontend stack using the project README or current docker/dev commands.
    2. Log in through the configured OIDC provider.
    3. Navigate to Document Types and create a document type with at least these fields: cliente.nombre, cliente.direccion.calle, cliente.contactos[].nombre, cliente.contactos[].telefono.
    4. Confirm the document type detail page shows cliente as an object, direccion as an object, contactos as an object-list, and the primitive leaves with their full canonical paths.
    5. Create or open a document design for that document type with an HTML template page that references nested/list tokens.
    6. Open generated preview mode, confirm mock JSON contains nested objects and contactos as an array of objects, edit at least one nested and one array value, and run preview.
    7. Confirm the returned PDF renders in the editor preview frame and reflects the edited nested/list values.
    8. Confirm no generated issuance appears as a side effect of preview; preview must use /preview, not /generate.
    9. Note any shared shell/navigation issue that overlaps with Phase 9's human_needed verification.
  </how-to-verify>
  <verify>
    <human-check>User completes the browser workflow and replies with approved or failure details.</human-check>
  </verify>
  <done>User approval confirms COMPUI-01 and COMPUI-02 browser behavior, or failure details are captured for gap planning.</done>
  <resume-signal>Type "approved" if all expected behavior passes, or describe the exact issue and screen/action where it failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human browser session -> API | Authenticated user exercises schema creation and preview endpoints. |
| Preview endpoint -> UI | Generated PDF bytes are rendered in iframe and must remain no-persistence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-09 | Tampering | End-to-end schema and mock payload flow | mitigate | Human UAT includes malformed/edited nested data path and confirms backend validation errors are surfaced. |
| T-10-10 | Information Disclosure | Generated preview PDF | mitigate | UAT confirms preview remains inside authenticated app flow and object URL behavior is used. |
| T-10-SC | Tampering | npm installs | mitigate | No package installs are planned; checkpoint verifies package-lock was not changed for Phase 10. |
</threat_model>

<verification>
Automated checks plus blocking browser UAT.
</verification>

<success_criteria>
Phase 10 is complete when automated checks pass and the human checkpoint confirms both COMPUI-01 and COMPUI-02 in a browser, with Phase 9 UAT risk recorded if still relevant.
</success_criteria>

<output>
Create `.planning/phases/10-complex-schema-ui-nested-data-previsualization/10-04-SUMMARY.md` when done.
</output>
