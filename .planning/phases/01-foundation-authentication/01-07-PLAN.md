---
phase: 01-foundation-authentication
plan: 07
type: execute
wave: 4
depends_on: ["01-03", "01-06"]
files_modified:
  - frontend/src/pages/LoginPage.tsx
  - frontend/src/pages/AuthenticatedShell.tsx
  - frontend/src/App.tsx
  - frontend/src/lib/api.ts
autonomous: true
requirements: [AUTH-01]

must_haves:
  truths:
    - "User can see a login screen with a Sign In button that redirects to the backend's OIDC login flow"
    - "After a successful login, the user reaches an authenticated shell showing their identity and a working Sign Out control"
    - "Visiting the authenticated shell without a valid session redirects to the login page, rather than silently rendering (AUTH-01 gating, UI path)"
    - "Login-failure and session-expired error states render the exact copy specified in 01-UI-SPEC.md"
  artifacts:
    - path: "frontend/src/pages/LoginPage.tsx"
      provides: "Precision Archival-styled login card: wordmark, tagline, Sign In button -> {VITE_API_BASE_URL}/auth/login, error banners for callback_failed/session_expired query params"
    - path: "frontend/src/pages/AuthenticatedShell.tsx"
      provides: "Topbar (wordmark + user email + Sign Out) + welcome heading/body, fetches /api/health on mount, redirects to /login?error=session_expired on 401"
  key_links:
    - from: "frontend/src/pages/LoginPage.tsx"
      to: "backend /auth/login"
      via: "window.location.href = `${API_BASE_URL}/auth/login`"
      pattern: "auth/login"
    - from: "frontend/src/pages/AuthenticatedShell.tsx"
      to: "backend /api/health"
      via: "apiFetch('/api/health')"
      pattern: "api/health"
    - from: "frontend/src/pages/AuthenticatedShell.tsx"
      to: "backend /auth/logout"
      via: "apiFetch('/auth/logout', {method: 'POST'})"
      pattern: "auth/logout"
---

<objective>
Wire the real Login page and Authenticated Shell against the now-working backend contract (01-06-PLAN's `/auth/login`, `/auth/logout`, `/api/health`), implementing the exact copy, colors, and layout specified in `01-UI-SPEC.md` on top of the scaffold and design tokens 01-03-PLAN already built.

Purpose: This is the user-visible proof of AUTH-01 — the screens a human actually clicks through in the 01-08-PLAN manual verification checkpoint.
Output: Working `LoginPage.tsx` and `AuthenticatedShell.tsx`, wired route guard in `App.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation-authentication/01-UI-SPEC.md
@.planning/phases/01-foundation-authentication/01-CONTEXT.md
</context>

<interfaces>
From 01-03-PLAN (frontend/src/lib/api.ts) — already on disk:
```typescript
export async function apiFetch(path: string, init?: RequestInit): Promise<Response>
// always sends credentials: "include"
```
From 01-06-PLAN (backend contract, verified live) — already on disk:
```
GET  {API_BASE_URL}/auth/login    -> 302 redirect to Keycloak
POST {API_BASE_URL}/auth/logout   -> {"status": "logged_out"}, clears session cookie
GET  {API_BASE_URL}/api/health    -> 200 {"sub": str, "email": str} if authenticated, 401 otherwise
Backend redirects failed /auth/callback to: {FRONTEND_ORIGIN}/login?error=callback_failed
```
From 01-UI-SPEC.md (Copywriting Contract — exact strings, do not paraphrase):
```
Login page heading: "Precision Archival"   (headline-xl, Hanken Grotesk)
Login page tagline: "Document Management Platform"   (label-caps, uppercase)
Primary CTA: "Sign In"
CTA subtext: "Sign in with your organization account to continue."
Error (callback failure) heading: "We couldn't sign you in."
Error (callback failure) body: "Something went wrong completing sign-in. Please try again, or contact your administrator if this keeps happening."
Error (callback failure) button: "Try Again"
Error (session expired): "Your session has expired. Please sign in again."
Authenticated welcome heading: "You're signed in"
Authenticated welcome body: "Document management features will appear here as they're built out. Come back once document types, templates, and the designer are live."
Sign-out control: "Sign Out"  (no confirmation dialog)
```
From 01-UI-SPEC.md (Color/Layout — exact values):
```
Login card: bg-surface-container-lowest, border border-outline-variant, rounded-lg, ~400px wide, centered, xl (32px) padding, lg (24px) gap between wordmark block and CTA block
Sign In button: bg-primary (#610000) text-white — only accent-colored element at rest
Shell topbar: bg-surface-container, wordmark left, email + Sign Out button right
Error banners: text/icon in error color (#ba1a1a)
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Login page — real copy, Sign In redirect, error states</name>
  <files>frontend/src/pages/LoginPage.tsx</files>
  <read_first>
    - frontend/src/pages/LoginPage.tsx (01-03-PLAN's placeholder — read current state before replacing)
    - frontend/tailwind.config.ts (01-03-PLAN — confirm exact token class names available: bg-primary, bg-surface-container-lowest, border-outline-variant, text-error, etc.)
    - .planning/phases/01-foundation-authentication/01-UI-SPEC.md (Copywriting Contract, Color, Layout Notes sections)
  </read_first>
  <action>
    Replace the placeholder with the real component:
    1. Read the `error` query param via `useSearchParams()` (react-router-dom). Map values to the exact copy from `<interfaces>` above: `"callback_failed"` -> heading "We couldn't sign you in.", body "Something went wrong completing sign-in. Please try again, or contact your administrator if this keeps happening.", button "Try Again" (clicking clears the query param, returning to the normal login state — does not need to call the backend). `"session_expired"` -> single line "Your session has expired. Please sign in again." rendered above the normal login card content (not a separate screen, per UI-SPEC).
    2. Render the centered card exactly per Layout Notes: `max-w-[400px] mx-auto bg-surface-container-lowest border border-outline-variant rounded-lg shadow-sm p-xl` (or Tailwind's arbitrary-value equivalent if `p-xl` isn't a registered padding utility — use `p-8` (32px) explicitly if the custom spacing scale only extended `spacing` values, not padding utilities directly; confirm against `tailwind.config.ts` from 01-03-PLAN which utility a `32px`/`24px` custom spacing key actually generates).
    3. Wordmark block: `<h1 className="font-headings text-2xl font-bold text-primary">Precision Archival</h1>` (headline-xl-equivalent per UI-SPEC: 32px/700/-0.02em — use the `headline-xl` scale, not `text-2xl`, if `tailwind.config.ts` registered `headline-xl` as a named font-size utility; otherwise use Tailwind arbitrary value `text-[32px] leading-[40px] tracking-[-0.02em]`), tagline `<p className="label-caps text-on-surface-variant uppercase">Document Management Platform</p>` (11px/700/uppercase/+0.05em tracking).
    4. Sign In button: `<button className="bg-primary text-white rounded px-lg py-sm w-full" onClick={() => { window.location.href = `${import.meta.env.VITE_API_BASE_URL}/auth/login`; }}>Sign In</button>`, with subtext below: "Sign in with your organization account to continue." in `text-on-surface-variant`.
    5. No JavaScript-side validation or loading spinner needed — this is a full-page redirect, not an async form submit.
  </action>
  <acceptance_criteria>
    - `frontend/src/pages/LoginPage.tsx` contains the exact string `"Sign in with your organization account to continue."`
    - `frontend/src/pages/LoginPage.tsx` contains `"We couldn't sign you in."` and `"Try Again"`
    - `frontend/src/pages/LoginPage.tsx` contains `"Your session has expired. Please sign in again."`
    - `frontend/src/pages/LoginPage.tsx` contains `/auth/login` and `VITE_API_BASE_URL`
    - `frontend/src/pages/LoginPage.tsx` contains `useSearchParams`
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>TypeScript compiles; visiting `/login`, `/login?error=callback_failed`, and `/login?error=session_expired` in `npm run dev` render the three distinct states with exact UI-SPEC copy and Precision Archival styling.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Authenticated shell — identity fetch, Sign Out, route guard</name>
  <files>frontend/src/pages/AuthenticatedShell.tsx, frontend/src/App.tsx, frontend/src/lib/api.ts</files>
  <read_first>
    - frontend/src/pages/AuthenticatedShell.tsx (01-03-PLAN's placeholder)
    - frontend/src/App.tsx (01-03-PLAN's route skeleton)
    - frontend/src/lib/api.ts (01-03-PLAN's apiFetch wrapper — extend, do not replace its signature)
    - .planning/phases/01-foundation-authentication/01-UI-SPEC.md (Copywriting Contract, Layout Notes — "do not build the full sidebar rail")
  </read_first>
  <action>
    1. `frontend/src/lib/api.ts` — add two thin named helpers on top of the existing `apiFetch`:
       ```typescript
       export interface CurrentUser { sub: string; email: string; }

       export async function fetchCurrentUser(): Promise<CurrentUser | null> {
         const res = await apiFetch("/api/health");
         if (res.status === 401) return null;
         if (!res.ok) throw new Error(`Unexpected status ${res.status}`);
         return res.json();
       }

       export async function logout(): Promise<void> {
         await apiFetch("/auth/logout", { method: "POST" });
       }
       ```
    2. `frontend/src/pages/AuthenticatedShell.tsx` — on mount (`useEffect`), call `fetchCurrentUser()`; if it resolves `null`, `navigate("/login?error=session_expired")` (react-router-dom's `useNavigate`); otherwise render:
       - Topbar: `bg-surface-container` full width, wordmark "Precision Archival" (left, `text-primary font-headings font-bold`), user's `email` + a "Sign Out" button (right) — plain secondary button styling, NOT accent-colored (per UI-SPEC: accent reserved for CTA only), no confirmation dialog. `onClick` calls `logout()` then `navigate("/login")`.
       - Body: heading "You're signed in" (`headline-lg`: 24px/700/-0.01em per UI-SPEC's Phase-1-local 2-weight exception), body copy "Document management features will appear here as they're built out. Come back once document types, templates, and the designer are live." (`body-md`).
       - Explicitly do NOT build the left sidebar rail (Documents/Assets/Templates) from `.design/repositorio_documentos.html` — those destinations don't exist until Phase 2+, per UI-SPEC's Layout Notes.
    3. `frontend/src/App.tsx` — no structural route change needed (still `/login` and `/`), but confirm `AuthenticatedShell` is the element at `/` (the guard logic lives inside the component per Task 2, not as a separate wrapper — acceptable for Phase 1's minimal scope, per UI-SPEC's explicit "no full sidebar shell yet" scoping).
  </action>
  <acceptance_criteria>
    - `frontend/src/lib/api.ts` contains `export async function fetchCurrentUser(` and `export async function logout(`
    - `frontend/src/pages/AuthenticatedShell.tsx` contains `"You're signed in"` and the exact body copy string `"Document management features will appear here as they're built out. Come back once document types, templates, and the designer are live."`
    - `frontend/src/pages/AuthenticatedShell.tsx` contains `"Sign Out"` and calls `logout()` 
    - `frontend/src/pages/AuthenticatedShell.tsx` contains `navigate("/login?error=session_expired")` for the unauthenticated case
    - `frontend/src/pages/AuthenticatedShell.tsx` does NOT contain any nav links to `/documents`, `/templates`, `/designer`, or similar not-yet-built destinations
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>TypeScript compiles; visiting `/` with no valid session redirects to `/login?error=session_expired`; with a valid session (manually testable once backend is live) shows the user's email, welcome copy, and a working Sign Out button.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` — no type errors.
- `cd frontend && npm run build` — succeeds.
- Manual spot-check (full end-to-end confirmed in 01-08-PLAN checkpoint): with backend running and no session cookie, visiting `http://localhost:5173/` redirects to `/login?error=session_expired`.
</verification>

<success_criteria>
- Login page and Authenticated Shell exactly match `01-UI-SPEC.md`'s copy, color, and layout contract.
- The frontend correctly distinguishes authenticated vs unauthenticated state by calling the real `/api/health` endpoint — no hardcoded/mocked auth state remains.
- Sign Out actually calls the backend logout endpoint (real session invalidation), not just a client-side redirect.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-authentication/01-07-SUMMARY.md`
</output>
