---
phase: 01-foundation-authentication
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/vite.config.ts
  - frontend/tailwind.config.ts
  - frontend/postcss.config.js
  - frontend/index.html
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/index.css
  - frontend/src/lib/api.ts
  - frontend/src/pages/LoginPage.tsx
  - frontend/src/pages/AuthenticatedShell.tsx
  - frontend/.env.example
autonomous: true
requirements: [AUTH-01]

must_haves:
  truths:
    - "The frontend is a decoupled Vite + React SPA (D-01, D-02) talking to the FastAPI backend, not a full-stack meta-framework"
    - "Tailwind is configured with the Precision Archival design system tokens (colors, typography, spacing, border-radius) from .design/DESIGN.md, not default Tailwind values"
    - "A routing skeleton exists distinguishing the logged-out (/login) and authenticated (/) views, ready for 04-01-PLAN to fill with real backend-wired behavior"
  artifacts:
    - path: "frontend/tailwind.config.ts"
      provides: "Tailwind theme extension with Precision Archival colors, typography (Hanken Grotesk/Inter/JetBrains Mono), spacing (4/8/12/16/24/32/48/64), borderRadius (0.125rem/0.25rem/0.375rem/0.5rem/0.75rem/9999px) per DESIGN.md"
    - path: "frontend/src/App.tsx"
      provides: "react-router-dom route switch: /login -> LoginPage, / -> AuthenticatedShell"
    - path: "frontend/src/lib/api.ts"
      provides: "fetch wrapper with credentials:'include' and VITE_API_BASE_URL base"
    - path: "frontend/src/pages/LoginPage.tsx"
      provides: "placeholder login page component (real copy/behavior wired in 04-01-PLAN)"
    - path: "frontend/src/pages/AuthenticatedShell.tsx"
      provides: "placeholder authenticated shell component (real copy/behavior wired in 04-01-PLAN)"
  key_links:
    - from: "frontend/src/lib/api.ts"
      to: "backend FastAPI (VITE_API_BASE_URL)"
      via: "fetch(url, {credentials: 'include'})"
      pattern: "credentials:\\s*['\"]include['\"]"
---

<objective>
Scaffold the frontend as a standalone Vite + React + TypeScript SPA (D-01, D-02, D-03) with the Precision Archival design system's tokens (colors, typography, spacing, radii) wired into Tailwind, plus a minimal routing skeleton and API client wrapper — the shell 04-01-PLAN will fill with the real login/authenticated-shell screens once the backend auth contract (03-01-PLAN) exists.

Purpose: Decouples "get the frontend build/tooling/design-tokens right" from "wire real auth behavior against a live backend contract," so this plan can run in Wave 1 fully parallel to the backend work.
Output: `frontend/` Vite React-TS project with Tailwind theme matching `.design/DESIGN.md`, routing skeleton, API client wrapper, placeholder pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-authentication/01-CONTEXT.md
@.planning/phases/01-foundation-authentication/01-RESEARCH.md
@.planning/phases/01-foundation-authentication/01-UI-SPEC.md
@.design/DESIGN.md
</context>

<interfaces>
<!-- Design tokens extracted verbatim from .design/DESIGN.md frontmatter — this IS the canonical source per 01-UI-SPEC.md. -->

```yaml
colors:
  background: '#fff8f6'          # surface / dominant 60%
  surface-container-lowest: '#ffffff'   # login card background
  surface-container: '#ffe9e6'          # shell topbar/panel background
  primary: '#610000'                     # accent 10% — Sign In button, wordmark, focus border
  error: '#ba1a1a'                       # destructive — error banners only
  on-surface: '#261816'                  # primary text
  on-surface-variant: '#5a403c'          # secondary/muted text
  secondary: '#5e5e5e'                   # tertiary/disabled-adjacent text
  outline: '#8e706b'                     # strong/focus-adjacent borders
  outline-variant: '#e3beb8'             # default hairline borders

typography:
  # font families
  headings: "'Hanken Grotesk', sans-serif"     # weight 700 only, per Phase 1 UI-SPEC (2-weight rule)
  body: "'Inter', sans-serif"                  # weights 400 (body) and 700 (label-caps)
  mono: "'JetBrains Mono', monospace"          # not used in Phase 1 UI but include in config for later phases
  # Phase 1 roles (exact values, see 01-UI-SPEC.md Typography table)
  body-md: { size: 14px, weight: 400, lineHeight: 20px }
  label-caps: { size: 11px, weight: 700, lineHeight: 16px, letterSpacing: 0.05em, transform: uppercase }
  headline-lg: { size: 24px, weight: 700, lineHeight: 32px, letterSpacing: -0.01em }
  headline-xl: { size: 32px, weight: 700, lineHeight: 40px, letterSpacing: -0.02em }

spacing:  # 4px base scale, per DESIGN.md + UI-SPEC extension
  base: 4px
  xs: 8px
  sm: 12px   # locked platform-wide exception — input padding, list-row padding
  md: 16px
  lg: 24px
  xl: 32px
  2xl: 48px  # UI-SPEC extension for Phase 1 centered login layout
  3xl: 64px  # UI-SPEC extension for Phase 1 centered login layout

borderRadius:  # per DESIGN.md `rounded:` block — canonical over the .design/*.html mockups' embedded config
  sm: 0.125rem
  DEFAULT: 0.25rem
  md: 0.375rem
  lg: 0.5rem
  xl: 0.75rem
  full: 9999px
```

Icon font: Material Symbols Outlined (Google Fonts), variable axes `FILL 0, wght 400, GRAD 0, opsz 24`.
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Vite + React + TypeScript scaffold with Tailwind design tokens</name>
  <files>frontend/package.json, frontend/vite.config.ts, frontend/tailwind.config.ts, frontend/postcss.config.js, frontend/index.html, frontend/src/main.tsx, frontend/src/index.css, frontend/.env.example</files>
  <read_first>
    - .design/DESIGN.md (frontmatter — colors, typography, spacing, rounded blocks)
    - .planning/phases/01-foundation-authentication/01-UI-SPEC.md (Design System, Spacing Scale, Typography, Color sections — confirms DESIGN.md is canonical over the .design/*.html mockups' embedded Tailwind config)
    - .planning/phases/01-foundation-authentication/01-RESEARCH.md (Standard Stack > Frontend table — react 19.2.7, vite 8.1.3, typescript 6.0.3, react-router-dom 7.18.1)
  </read_first>
  <action>
    1. Scaffold via `npm create vite@latest frontend -- --template react-ts` from the repo root, then `cd frontend && npm install react-router-dom`.
    2. `frontend/tailwind.config.ts` — extend Tailwind's theme with the exact tokens listed in this plan's `<interfaces>` block: `colors` (background, surface-container-lowest, surface-container, primary, error, on-surface, on-surface-variant, secondary, outline, outline-variant — use these exact key names so components can use `bg-primary`, `text-on-surface`, etc.), `fontFamily` (headings: Hanken Grotesk, body: Inter, mono: JetBrains Mono), `spacing` (add custom keys `xs: '8px', sm: '12px', md: '16px', lg: '24px', xl: '32px', '2xl': '48px', '3xl': '64px'` alongside Tailwind's default scale — do not remove Tailwind's defaults, extend them), `borderRadius` (sm: 0.125rem, DEFAULT: 0.25rem, md: 0.375rem, lg: 0.5rem, xl: 0.75rem — matches DESIGN.md exactly, NOT the `.design/*.html` mockups' embedded config which uses different values per 01-UI-SPEC.md's explicit warning).
    3. `frontend/postcss.config.js` — standard Tailwind + Autoprefixer setup.
    4. `frontend/index.html` — add Google Fonts `<link>` tags for Hanken Grotesk (weights 600, 700), Inter (weights 400, 700), JetBrains Mono (weight 400), and Material Symbols Outlined (`FILL 0, wght 400, GRAD 0, opsz 24` variable axes via the standard Google Fonts icon-font URL pattern `family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@24,400,0,0`).
    5. `frontend/src/index.css` — `@tailwind base; @tailwind components; @tailwind utilities;` plus a `body { @apply bg-background text-on-surface font-body; }` base rule.
    6. `frontend/src/main.tsx` — standard Vite React entrypoint rendering `<App />` inside `<BrowserRouter>` (from react-router-dom) inside `<React.StrictMode>`.
    7. `frontend/.env.example` — `VITE_API_BASE_URL=http://localhost:8000`.
  </action>
  <acceptance_criteria>
    - `frontend/package.json` contains `"react-router-dom"` and `"react": "^19` (or resolved 19.x version)
    - `frontend/tailwind.config.ts` contains `primary` and `#610000`
    - `frontend/tailwind.config.ts` contains `'2xl': '48px'` and `'3xl': '64px'`
    - `frontend/tailwind.config.ts` contains `lg: '0.5rem'` (or `0.5rem` under borderRadius.lg — NOT `0.25rem`, which would be the mockups' outdated draft value)
    - `frontend/index.html` contains `Hanken+Grotesk` and `Material+Symbols+Outlined`
    - `frontend/.env.example` contains `VITE_API_BASE_URL=`
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>`npm run build` completes successfully producing a `frontend/dist/` bundle; Tailwind config contains all Precision Archival tokens from `.design/DESIGN.md`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Routing skeleton, API client wrapper, placeholder pages</name>
  <files>frontend/src/App.tsx, frontend/src/lib/api.ts, frontend/src/pages/LoginPage.tsx, frontend/src/pages/AuthenticatedShell.tsx</files>
  <read_first>
    - frontend/src/main.tsx (Task 1 of this plan — confirms BrowserRouter wraps App)
    - frontend/tailwind.config.ts (Task 1 of this plan — confirms available token names to use in placeholder markup)
  </read_first>
  <action>
    1. `frontend/src/lib/api.ts` — export a typed fetch wrapper:
       ```typescript
       const API_BASE_URL = import.meta.env.VITE_API_BASE_URL as string;

       export async function apiFetch(path: string, init: RequestInit = {}): Promise<Response> {
         return fetch(`${API_BASE_URL}${path}`, { ...init, credentials: "include" });
       }
       ```
       This is the ONLY function later plans (04-01-PLAN) use to talk to the backend — always includes `credentials: "include"` so the httpOnly session cookie is sent (per D-08 and RESEARCH.md Pitfall 5 on same-site cookies).
    2. `frontend/src/pages/LoginPage.tsx` — placeholder functional component rendering a centered card (`bg-surface-container-lowest`, `rounded-lg`, `border border-outline-variant`) with static text "Precision Archival" (`font-headings text-2xl` — real copy/error-state wiring happens in 04-01-PLAN). No backend calls yet.
    3. `frontend/src/pages/AuthenticatedShell.tsx` — placeholder functional component rendering a topbar (`bg-surface-container`) with static text "You're signed in" (real copy/backend-fetch/Sign Out wiring happens in 04-01-PLAN). No backend calls yet.
    4. `frontend/src/App.tsx` — route switch using `react-router-dom`'s `<Routes>`/`<Route>`:
       ```tsx
       <Routes>
         <Route path="/login" element={<LoginPage />} />
         <Route path="/" element={<AuthenticatedShell />} />
       </Routes>
       ```
       No auth-guard logic yet (both routes render unconditionally) — 04-01-PLAN adds the redirect-on-401 guard once `/api/health` exists.
  </action>
  <acceptance_criteria>
    - `frontend/src/lib/api.ts` contains `credentials: "include"` (or `'include'`)
    - `frontend/src/lib/api.ts` contains `export async function apiFetch(`
    - `frontend/src/App.tsx` contains `path="/login"` and `path="/"`
    - `frontend/src/pages/LoginPage.tsx` and `frontend/src/pages/AuthenticatedShell.tsx` both exist as valid TSX components (default export)
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>TypeScript compiles with no errors; visiting `/login` and `/` in `npm run dev` renders the two placeholder pages with Precision Archival tokens applied (not default browser styling).</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run build` succeeds.
- `cd frontend && npx tsc --noEmit` reports no type errors.
- Visual spot-check: `npm run dev`, visit `http://localhost:5173/login` and `http://localhost:5173/` — both render with the Precision Archival background (`#fff8f6`) and fonts loaded, not default Vite template styling.
</verification>

<success_criteria>
- Frontend builds and type-checks cleanly as a standalone Vite SPA.
- Tailwind config exactly encodes `.design/DESIGN.md`'s tokens (colors, typography, spacing, radii), not default Tailwind or the outdated mockup-embedded config.
- Routing skeleton and `apiFetch` wrapper are ready for 04-01-PLAN to wire real backend-driven behavior into, without needing to touch build tooling again.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-authentication/01-03-SUMMARY.md`
</output>
