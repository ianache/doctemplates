---
phase: 01-foundation-authentication
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - postgres/init/001-create-test-db.sql
  - keycloak/import/docmanagement-realm.json
  - .env.example
autonomous: true
requirements: [AUTH-01]

must_haves:
  truths:
    - "A self-hosted Keycloak instance (D-05) and a Postgres instance (D-10) can both be brought up locally with a single command"
    - "The Keycloak realm boots pre-configured — a confidential client for the browser login flow, a client-credentials client for future machine-to-machine callers, an audience protocol mapper, and at least two distinct test users — with no manual admin-console clicking required"
    - "Two distinct test users exist so AUTH-01's 'multiple distinct users can each authenticate under their own identity' can actually be exercised"
  artifacts:
    - path: "docker-compose.yml"
      provides: "keycloak + postgres services for local dev"
    - path: "keycloak/import/docmanagement-realm.json"
      provides: "docmanagement realm: docmanagement-backend confidential client (PKCE), docmanagement-api-client client-credentials client, audience mapper, 2 test users"
    - path: "postgres/init/001-create-test-db.sql"
      provides: "creates the docmanagement_test database alongside docmanagement on container init"
  key_links:
    - from: "docker-compose.yml"
      to: "keycloak/import/docmanagement-realm.json"
      via: "--import-realm + volume mount"
      pattern: "import-realm"
---

<objective>
Stand up local dev infrastructure: a Docker Compose stack running Keycloak (D-05, self-hosted OIDC provider) and PostgreSQL (D-10), with Keycloak's realm — client(s), audience mapper, and test users — bootstrapped automatically via realm-export JSON so no manual admin-console configuration is ever required to develop or test this phase.

Purpose: 01-01-PLAN's Settings/models assume this stack exists; 02-01/02-02/03-01-PLAN's auth logic and tests need a real (or realistically mockable) Keycloak realm to validate against.
Output: `docker-compose.yml`, `postgres/init/001-create-test-db.sql`, `keycloak/import/docmanagement-realm.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-authentication/01-CONTEXT.md
@.planning/phases/01-foundation-authentication/01-RESEARCH.md
</context>

<interfaces>
<!-- Env vars / identifiers other plans (02-01, 02-02, 03-01, 05-01 checkpoint) will reference by these exact names. -->

Realm name: `docmanagement`
Clients:
- `docmanagement-backend` — confidential client, Standard Flow (Authorization Code) enabled, PKCE `S256` required, valid redirect URI `http://localhost:8000/auth/callback`, web origins `http://localhost:5173`. Client secret must be copied into `.env`'s `OIDC_CLIENT_SECRET` after first `docker compose up` (Keycloak generates it; the realm-export JSON pins a fixed dev secret so this step is not required in local dev — see Task 2).
- `docmanagement-api-client` — confidential client, Service Accounts enabled (client-credentials grant), Standard Flow disabled — reserved for Phase 6 machine-to-machine callers, created now so the realm shape doesn't change later.

Audience protocol mapper: attached to `docmanagement-backend`, type "Audience", "Included Client Audience" = `docmanagement-backend` — ensures tokens carry `"aud": "docmanagement-backend"` (addresses RESEARCH.md Pitfall 1 — Keycloak's default `aud` does not include the requesting client without this).

Test users (both with temporary, non-expiring dev passwords set directly in the realm-export JSON — this is a local dev realm only, never used in prod):
- `alice@example.com` / password `devpass123`
- `bob@example.com` / password `devpass123`

Test databases (Postgres): `docmanagement` (app) and `docmanagement_test` (pytest, per `TEST_DATABASE_URL` from 01-01-PLAN).
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Docker Compose stack (Keycloak + Postgres) with test-DB bootstrap</name>
  <files>docker-compose.yml, postgres/init/001-create-test-db.sql</files>
  <read_first>
    - .planning/phases/01-foundation-authentication/01-RESEARCH.md (Code Examples > Keycloak local dev container)
    - .planning/phases/01-foundation-authentication/01-CONTEXT.md (D-05, D-10)
  </read_first>
  <action>
    1. `docker-compose.yml` at repo root with two services:
       ```yaml
       services:
         keycloak:
           image: quay.io/keycloak/keycloak:26.6
           command: start-dev --import-realm
           environment:
             KC_BOOTSTRAP_ADMIN_USERNAME: admin
             KC_BOOTSTRAP_ADMIN_PASSWORD: change_me
           ports:
             - "127.0.0.1:8080:8080"
           volumes:
             - ./keycloak/import:/opt/keycloak/data/import
         postgres:
           image: postgres:16
           environment:
             POSTGRES_USER: docmanagement
             POSTGRES_PASSWORD: change_me
             POSTGRES_DB: docmanagement
           ports:
             - "127.0.0.1:5432:5432"
           volumes:
             - ./postgres/init:/docker-entrypoint-initdb.d
             - pgdata:/var/lib/postgresql/data
       volumes:
         pgdata:
       ```
       Pin `quay.io/keycloak/keycloak:26.6` (matches the version RESEARCH.md's Sources section found as the current release, 26.6.4) rather than `:latest`, for reproducibility.
    2. `postgres/init/001-create-test-db.sql`:
       ```sql
       CREATE DATABASE docmanagement_test OWNER docmanagement;
       ```
       (Postgres's official image runs every `.sql`/`.sh` file in `/docker-entrypoint-initdb.d` on first container init, after the `POSTGRES_DB` database is created — this creates the second database `docmanagement_test` alongside it.)
  </action>
  <acceptance_criteria>
    - `docker-compose.yml` contains `image: quay.io/keycloak/keycloak:26.6` and `command: start-dev --import-realm`
    - `docker-compose.yml` contains `image: postgres:16`
    - `docker-compose.yml` mounts `./keycloak/import:/opt/keycloak/data/import` and `./postgres/init:/docker-entrypoint-initdb.d`
    - `postgres/init/001-create-test-db.sql` contains `CREATE DATABASE docmanagement_test`
  </acceptance_criteria>
  <verify>
    <automated>docker compose config --quiet && echo "compose file valid"</automated>
  </verify>
  <done>`docker compose config` validates the file with no errors; bringing the stack up with `docker compose up -d` starts both containers (manually confirmed in the Wave-5 checkpoint, not required to pass here since Keycloak's realm import happens in Task 2).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Keycloak realm-export bootstrap (clients, audience mapper, test users)</name>
  <files>keycloak/import/docmanagement-realm.json, .env.example</files>
  <read_first>
    - docker-compose.yml (Task 1 of this plan — confirm the import volume path matches)
    - .planning/phases/01-foundation-authentication/01-RESEARCH.md (Common Pitfalls > Pitfall 1: Keycloak audience mapper)
    - .env.example (repo root, created by 01-01-PLAN — add to it, do not overwrite existing entries)
  </read_first>
  <action>
    Create `keycloak/import/docmanagement-realm.json` — a Keycloak realm-export JSON with `"realm": "docmanagement"`, `"enabled": true`, and:

    1. Client `docmanagement-backend`:
       ```json
       {
         "clientId": "docmanagement-backend",
         "enabled": true,
         "publicClient": false,
         "secret": "dev-backend-secret-change-in-prod",
         "standardFlowEnabled": true,
         "directAccessGrantsEnabled": false,
         "serviceAccountsEnabled": false,
         "redirectUris": ["http://localhost:8000/auth/callback"],
         "webOrigins": ["http://localhost:5173"],
         "attributes": { "pkce.code.challenge.method": "S256" },
         "protocolMappers": [
           {
             "name": "audience-mapper",
             "protocol": "openid-connect",
             "protocolMapper": "oidc-audience-mapper",
             "config": {
               "included.client.audience": "docmanagement-backend",
               "id.token.claim": "false",
               "access.token.claim": "true"
             }
           }
         ]
       }
       ```
    2. Client `docmanagement-api-client` (reserved for Phase 6, created now for realm-shape stability):
       ```json
       {
         "clientId": "docmanagement-api-client",
         "enabled": true,
         "publicClient": false,
         "secret": "dev-api-client-secret-change-in-prod",
         "standardFlowEnabled": false,
         "serviceAccountsEnabled": true,
         "directAccessGrantsEnabled": false
       }
       ```
    3. Two realm users under `"users"`:
       ```json
       [
         {"username": "alice@example.com", "email": "alice@example.com", "enabled": true, "emailVerified": true,
          "credentials": [{"type": "password", "value": "devpass123", "temporary": false}]},
         {"username": "bob@example.com", "email": "bob@example.com", "enabled": true, "emailVerified": true,
          "credentials": [{"type": "password", "value": "devpass123", "temporary": false}]}
       ]
       ```
    4. Update `.env.example` (repo root, append — do not remove existing entries from 01-01-PLAN): set the example values to match this realm exactly:
       ```
       OIDC_ISSUER=http://localhost:8080/realms/docmanagement
       OIDC_CLIENT_ID=docmanagement-backend
       OIDC_CLIENT_SECRET=dev-backend-secret-change-in-prod
       OIDC_API_AUDIENCE=docmanagement-backend
       ```
       (These match `.env.example` placeholders from 01-01-PLAN — this task fills in realistic realm-matching values instead of the generic placeholders, since the realm now exists.)
  </action>
  <acceptance_criteria>
    - `keycloak/import/docmanagement-realm.json` contains `"realm": "docmanagement"`
    - `keycloak/import/docmanagement-realm.json` contains `"clientId": "docmanagement-backend"` and `"clientId": "docmanagement-api-client"`
    - `keycloak/import/docmanagement-realm.json` contains `"oidc-audience-mapper"`
    - `keycloak/import/docmanagement-realm.json` contains both `"alice@example.com"` and `"bob@example.com"`
    - `.env.example` contains `OIDC_ISSUER=http://localhost:8080/realms/docmanagement`
  </acceptance_criteria>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('keycloak/import/docmanagement-realm.json','utf8')); console.log('valid json')"</automated>
  </verify>
  <done>The realm-export JSON is valid JSON containing both clients, the audience mapper, and both test users; `.env.example` reflects realm-matching values.</done>
</task>

</tasks>

<verification>
- `docker compose config --quiet` validates with no errors.
- `node -e "JSON.parse(require('fs').readFileSync('keycloak/import/docmanagement-realm.json','utf8'))"` parses without error.
- Manual (deferred to 05-01 checkpoint): `docker compose up -d` brings up Keycloak reachable at `http://localhost:8080` with the `docmanagement` realm already present (no manual realm creation needed), and Postgres reachable at `localhost:5432` with both `docmanagement` and `docmanagement_test` databases existing.
</verification>

<success_criteria>
- A single `docker compose up -d` brings up a fully pre-configured Keycloak realm (2 clients, audience mapper, 2 test users) and Postgres (2 databases) with zero manual admin-console steps.
- `.env.example` values exactly match the bootstrapped realm's client ID/secret/issuer, so copying it to `.env` and running the backend works without hunting for values in the Keycloak admin UI.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-authentication/01-02-SUMMARY.md`
</output>
