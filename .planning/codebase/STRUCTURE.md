# Codebase Structure

**Analysis Date:** 2026-07-05

## Directory Layout

```
29-DocManagemet/
├── .claude/              # Claude Code settings
├── .planning/            # Planning documents (auto-generated)
├── .venv/                # Python virtual environment (not committed)
├── main.py              # Entry point stub
├── pyproject.toml       # Python project configuration
├── uv.lock              # Dependency lock file
├── README.md            # Project description (empty)
├── PRD.md               # Product requirements document
└── .python-version      # Python version specification
```

## Directory Purposes

**`.planning/codebase/`:**
- Purpose: Auto-generated codebase analysis documents
- Contains: ARCHITECTURE.md, STRUCTURE.md, CONVENTIONS.md, TESTING.md

**`.claude/`:**
- Purpose: Claude Code project-specific settings
- Contains: settings.local.json configuration file

## Key File Locations

**Entry Points:**
- `main.py`: Only current entry point (stub implementation)

**Configuration:**
- `pyproject.toml`: Project metadata, dependencies (headroom-ai), Python version requirement (>=3.12)
- `.python-version`: Python runtime version specification
- `uv.lock`: Dependency lock file for reproducible installs

**Documentation:**
- `PRD.md`: Comprehensive product requirements and API specifications for intended features
- `README.md`: Project description (currently empty)

## Current File Inventory

**Active Source Code:**
- `main.py` (5 lines) - Minimal stub with main() function

**Configuration & Metadata:**
- `pyproject.toml` - Declares headroom-ai dependency
- `uv.lock` - Locked dependency versions
- `.python-version` - Python version constraint

**Documentation:**
- `PRD.md` - Complete product specification (intended features not yet implemented)
- `README.md` - Empty placeholder

## Naming Conventions

**Files:**
- Lowercase with underscores for Python modules (currently only `main.py`)
- UPPERCASE.md for documentation files

**Directories:**
- Lowercase with hyphens or no separator (`.claude`, `.planning`)
- Expected: `src/` or `app/` (not yet created)

## Where to Add New Code

**New Feature Implementation:**
- Create `src/` directory structure to house application code
- Suggested layout based on PRD requirements:
  ```
  src/
  ├── api/           # API endpoints (GET/POST /api/bff/maquetas/*)
  ├── models/        # Data models (Template, Page, Marker)
  ├── services/      # Business logic (marker filtering, PDF composition)
  └── utils/         # Utilities (PDF manipulation, token replacement)
  ```

**Tests:**
- Tests should be co-located: `src/*/test_*.py` or separate `tests/` directory
- See TESTING.md for testing patterns

**Utilities & Helpers:**
- Shared helpers: `src/utils/` directory

## Special Directories

**`.planning/codebase/`:**
- Purpose: Codebase analysis documents (auto-generated by GSD mapping agents)
- Generated: Yes
- Committed: Yes (part of project planning artifacts)

**`.venv/`:**
- Purpose: Python virtual environment
- Generated: Yes (via `uv venv` or `python -m venv`)
- Committed: No (in .gitignore)

## Getting Started Structure

**To start implementation:**

1. Create `src/` directory: `mkdir src`
2. Create core modules based on PRD requirements:
   - `src/models.py` - Define Template, Page, Marker data classes
   - `src/api.py` - Define API endpoints (FastAPI suggested for Python BFF)
   - `src/services.py` - Implement marker filtering and PDF composition logic
3. Move or enhance `main.py` to initialize and run the application
4. Create `tests/` or `test_*.py` files alongside implementation
5. Update `README.md` with setup and usage instructions

---

*Structure analysis: 2026-07-05*
