// Placeholder login page. Real copy, form fields, and error-state wiring
// against the backend auth contract happen in 01-07-PLAN.
export default function LoginPage() {
  return (
    <div className="flex min-h-screen items-center justify-center bg-background">
      <div className="rounded-lg border border-outline-variant bg-surface-container-lowest p-xl">
        <h1 className="font-headings text-2xl font-bold text-on-surface">
          Precision Archival
        </h1>
      </div>
    </div>
  );
}
